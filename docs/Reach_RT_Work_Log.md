# Reach-RT 作業ログ

現在の状態と次の作業：[Reach_RT_Implementation_Progress.md](Reach_RT_Implementation_Progress.md)  
研究仕様：[Reach_RT_Research_Spec.md](Reach_RT_Research_Spec.md)

このファイルは実際の作業履歴を追記する。過去の結果は、その時点のコードと環境に対する結果として保存する。訂正する場合も訂正日と理由を残す。

## E001 — 2026-09-21：実装手順と進捗管理の開始

### 対象

G0-01、G0-02、G0-03の開発環境確認部分。

### 行ったこと

1. 研究仕様v0.1.0と既存コードを照合した。
2. Reach-RT固有のモジュール名と研究モードを、主要ソース・文書・プロジェクト設定から検索した。
3. Loopback、H.264設定、期限付き回復、回線模擬、実験記録、表示基盤を確認した。
4. Visual Studio Installerの`vswhere`でMSBuildのインストール先を確認した。
5. G0～G6の38タスク、各完了条件、更新ルールを記載した進捗管理ファイルを作成した。
6. 仕様書に進捗管理と作業ログへの参照を追加した。

### 確認したソース状態

- HEAD：`467b475`（2026-06-13のmerge commit）。
- 本作業前から存在した追跡済み変更：`README.md`、`application/AppMain.cpp`、`network/FrameReassembler.cpp`、`network/NetworkCsvLogger.cpp`、`network/NetworkManager.cpp`、`network/NetworkManager.h`、`network/NetworkStats.h`、`network/UdpReceiver.cpp`、`network/UdpReceiver.h`。
- 本作業前から存在した未追跡文書：BlindDrop関連3文書と`docs/Reach_RT_Research_Spec.md`。
- 未コミット変更を含むため、HEADだけでは現行実装を再現できない。基準性能の記録時に差分・実行ファイル・設定のハッシュも保存する。

### 開発環境の確認結果

| 項目 | 結果 |
|---|---|
| Visual Studioの所在 | `C:\Program Files\Microsoft Visual Studio\18\Community` |
| MSBuildの所在 | `C:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe` |
| プロジェクトの要求 | v145 / Windows SDK 10.0.26100.0 / x64 / C++20 |
| 既定の出力先 | `$(SolutionDir)..\generated\outputs\$(Configuration)\` |
| `msbuild` / `cl`のPATH検索 | 今回のPowerShellでは解決されなかった。コンパイラ未導入を意味しない |
| 必要コンポーネント全体 | 未確認 |
| 実ビルド | 未実施 |
| アプリ実行・性能測定 | 未実施 |

### 得られた結論

- 映像通信エンジンの基盤はコード上に存在する。
- Reach-RT専用の仮想作業、受信映像による閉ループ、容量付き双方向回線、専用スケジューラの実装は、今回の確認範囲で見つかっていない。
- 研究仕様の作成済み状態と、研究システムの実装・性能検証済み状態を分けて管理する必要がある。
- 現在地をG0途中、研究ゲート通過0 / 7とした。G0-01とG0-02は完了、G0-03は作業中。

### 成果物と確認

- 作成：`docs/Reach_RT_Implementation_Progress.md`。
- 作成：`docs/Reach_RT_Work_Log.md`。
- 更新：`docs/Reach_RT_Research_Spec.md`の進捗管理への参照。
- 文書内の参照先、タスクIDの一意性、38件の状態集計、UTF-8、Markdownコードフェンスを確認。
- 本エントリで研究コードの実装完了や、実験合格を報告していない。

### 残りの作業・次の一手

G0-03として、出力先・依存関係・ビルド環境を確認し、既存版のビルド結果を保存する。続いてG0-04の基準動作・性能記録へ進む。

## E002 — 2026-09-21：G0のビルドと既存版基準試験

対象ID：G0-03、G0-04。着手時は環境の所在のみ確認済み、実ビルド・実行は未実施。

### 実装意図と変更

- 元の未コミット変更9ファイルを維持し、既存エンジンが実行可能かを先に確認した。
- `tools/build_reach_g0.ps1`と`reach_g0_paths.props`で、ビルド出力を作業領域内に分離した。
- `tools/run_reach_g0_baseline.py`で、試行ごとのResources/config、ログ、差分、ソース/実行ファイルSHA-256を保存するようにした。実験条件の取り違えと過去ログの上書きを防ぐためである。
- `tools/summarize_reach_g0.py`で、事前指定の正常/10%損失×3 seedだけを集計した。

### 結果

- v145、SDK 10.0.26100.0、Release x64のビルド成功。基準版は警告28・エラー0。
- 初期失敗はMSBuild子プロセス環境のPath/PATH重複。Pythonによる環境名正規化で解消。並列数は1。マシン設定は変更しなかった。
- カメラなし、生成H.264、loopback、20秒、warmup 5秒、seed 101/202/303で正常と10%損失を逐次実行。6回とも終了コード0、シナリオ一致、復号ログあり。
- 各試行平均表示fpsの平均は正常18.91、損失14.98。既存適応処理での基準値であり、Reach-RTの効果ではない。
- CPU i7-13620H、メモリ約15.71 GiB、Intel UHD/RTX 4060 Laptop GPU。符号化autoはNVIDIA MFT有効化失敗後Intel Quick Syncを選択。

### 証拠・制約

証拠は`artifacts/reach_g0_20260921/`の`build_Release_baseline*`、`machine.json`、`baseline_summary.json`、`runs/baseline_{101,202,303}`と`runs/loss10_{101,202,303}`。基準exeを`bin/Release/GE3_baseline.exe`として保存した。SHA-256はG0確認書3章に記載。

WMI/CIMはアクセス不可のためレジストリ/DXGI/native memory APIで代替。電源設定やOSスケジューリングは固定していない。計測窓はログ上約14.01秒。既存遅延のフレーム対応に問題が見つかったため、遅延の研究結果としては採用しない（E003）。試行数3から優位性を推定しない。

G0-03、G0-04を完了へ更新。基準試験時点の実行ファイルと、その後の診断追加版を別に記録する。

## E003 — 2026-09-21：実コーデック検査と時刻対応問題の発見

対象ID：G0-05。設定値だけから参照構造・低遅延性を仮定しないことを目的とした。

### 実装と確認

- `tools/reach_g0_codec_probe.cpp/.vcxproj`で既存H264Encoderに640×360 NV12、1.5Mbps、30fps、90枚を入力。入力番号45前にIDR要求し、出力AUと要求履歴を保存。
- `tools/inspect_reach_g0_codec.py`でAnnex-B/SPSの一部/slice種別を検査。参照グラフ全体は未検証と明示。
- `tools/run_reach_g0_codec.py`でビルド・auto/software実行・ハッシュ・構文解析を再実行可能にした。`codec_repro_check`に最終実行を保存。
- `network/NetworkVideoReceiver.h/.cpp`でMFTの出力PTSを取得し、受信CSVに入力PTS/出力PTS/有効性を追加。
- 診断版をビルドし、正常12秒の`pts_audit_auto`を実行。`tools/summarize_reach_g0_pts.py`で集計。

### 実測と判断

1. auto/softwareとも90 AU、I 4/P 86/B 0、SPS最大参照数1、検査範囲の構文エラー0。
2. IDR出力通番はauto 0/28/45/73、software 0/30/45/75。要求と実IDRを別イベントとして記録した。
3. softwareの最初の出力は入力呼出16、再確認時約539ms。低遅延設定/Bなしでも出力バッファリングがある。
4. 受信時刻監査は記録対象の復号成功180件中180件で出力PTSが入力より50ms古い。現在入力のID/時刻を返った画像へ付けるコード上の仮定が成立しないことを確認した。
5. 実codecは保守的IDR世代モデルを採用。単純IP参照は理論モデルの仮定に限定する。

### 失敗・未完了事項

初版のcodec probeはDrainOutputの待ちタイムアウトを失敗と数えた。既存APIの意味を追跡し、入力/flush失敗と「イベントなしまたはエラー」を分離した。修正前結果も保存した。構文解析器のSP/SIラベルも修正した。

時刻の対応付け自体は未修正。完全なPPS/MMCO/参照リスト解析も未実施。G0で不具合を確認したことを、修正完了としない。G1-03でencoder/decoderの出力PTSから元の撮影を照合する台帳と画像内IDによる検証、G2-04で欠損・再同期の検証を行う。

G0-05は「制約と採用モデル・観測方法の確定」として完了。G1-03の完了条件を具体化した。診断追加版の実行結果をE002の6試行へ混ぜていない。

## E004 — 2026-09-21：比較方式・初期タスク・G0判定の確定

対象ID：G0-06、G0-07。

### 行ったことと意図

- Hairpin本文と訂正/公式実装、Tooth本文、TAMS、Age of Loop、Viabilityの関連節を確認した。既知のアイデアを新規と主張せず、実装差を隠さないためである。
- B1の期限計画、B2の復号情報追加、B3の作業情報追加と共通候補・共通予算を具体化した。原論文再現ではない独自方式として表示する。RSとXORの能力差はG3で解消するか結論を限定する。
- T1/T2の座標・マーカー・姿勢推定・制御則・制動・ウォッチドッグ・真値境界をINITIALとして決定した。現時点で閉ループ動作済みとはしていない。
- `docs/Reach_RT_G0_Assessment.md`を作成。機能の目的、実装状態、実測の限界、対外説明例、再実行方法、G1への条件をまとめた。
- `docs/reach_rt_initial.json`を作成。現アプリ未対応の設計資料と明示した。
- 研究仕様をv0.1.1へ更新し、G0確認書への参照、保守的モデル、PTS照合の必須条件を追記した。

### 最終確認

Python 5ファイルの構文、PowerShellの構文、追加props/vcxprojのXML、初期設定JSON、18初期姿勢の組合せ、文書のUTF-8・コードフェンス・ローカル参照、38件の一意なタスクIDと完了7件を確認した。`git diff --check`はエラーなし（既存の改行変換通知あり）。機械可読な検査結果は`artifacts/reach_g0_20260921/final_checks.json`に保存。C++の変更は診断版Releaseビルドと`pts_audit_auto`、プローブは`codec_repro_check`で動作確認した。設計した制御器の動作試験はG1で行う。

### 現在地と次の作業

G0-06/07の設計作業を完了、G0全7項目を完了とした。研究ゲートは1/7。ビルド・基準動作・コーデック制約・初期タスク・比較方針が明確というG0条件を満たした。時刻誤対応を放置して研究測定へ進むことは認めない。

次はG1-01の研究設定・共通時計・ログとG1-02/03の世界・撮影対応。その後、受信画素による固定制御と逆方向UDPを接続し、理想条件の18初期姿勢で検証する。研究の改善率、成功率、2ms計算目標はいずれも未検証。

## E005 — 2026-09-21：G1-01研究モードの実装と検証

対象ID：G1-01。着手時は未実装。今回の区切りは研究モードの土台までとし、G1-02～06は未着手のまま管理する。

### 何を、なぜ実装したか

- 同じGE3.exeの`AppMain::Run`で研究モードを分岐する入口を追加した。既存実験と研究用の条件・結果を混同しないためである。
- `research/ReachJson.*`と`ReachFoundation.*`に、上限付きJSON読取り、厳密な設定検査、共通単調時計、周期期限、GUID、SHA-256、セッション記録を実装した。
- `ReachResearchMode.cpp`に、画面あり/なしの実行、周期超過判定、状態と時計通知数を表示する基盤画面を追加した。
- `config/reach_rt_g1_foundation.json`は、今動く機能だけの実行用設定とした。G0設計JSONの未実装値を読み込んだように見せない。
- `tools/run_reach_g1.py`、`open_reach_g1.cmd`を追加。実行ごとの条件・差分・新規研究ソース・実行結果を保存する。
- `test_reach_g1.cpp`、`reach_g1_tests.vcxproj`、`verify_reach_g1.py`、`verify_reach_g1_legacy.py`を追加し、設定誤り・時刻・同時記録・実際のモード分岐を検証可能にした。
- `docs/Reach_RT_G1_Foundation.md`に対外説明例、実装意図、構成、結果の意味、起動方法、未完了項目をまとめた。

### 検証した状態と結果

- HEADは`467b475`、以前からの未コミット変更を維持。今回は新規researchファイル、プロジェクト登録、AppMainの入口6行を追加した。H.264/復号対応のコードは今回変更していない。
- Release x64ビルド成功。新規コードの型変換警告修正後の増分ビルドは警告0/エラー0。クリーンビルド全体の警告0を意味しない。
- exe SHA-256：`446683a698684cd972248dd09800d03ab15f96f1570455a063ccf158acc7b495`。
- 単体検証：合格（663アサーション。大半は記録各行のID/連番/時刻検査）。30 Hzを1時間相当進めて108,000通知、4スレッド計200イベントの欠落なし。壁時計1時間の耐久試験ではない。
- 実アプリ統合12ケース：正常T1/T2ラベル、不正モード・設定8ケース、同時起動2ケースを確認。設定/exeハッシュ、時計通知の集計、連番、session分離を検証。
- 画面なし2秒実行：physics 200/camera 60/control 40通知、正常終了。
- 画面あり2秒実行：ウィンドウ作成経路を通り、同数の通知と`foundation_completed`を記録。完了後の画面は閉じるまで残す設計。
- 既存モード確認：同じexeを`legacy`で8秒実行。終了コード0、記録対象の復号124件、表示集計82枚。回帰の短い動作確認であり性能比較ではない。

### 失敗・制約

- PowerShellの実行ポリシーにより最初のスクリプト起動が拒否された。許可を得て、該当プロセスだけExecutionPolicy Bypassでビルドした。全体のポリシー設定は変更していない。
- テスト初版のinitializer list型推論エラー、GUIDの型変換警告を修正した。
- 画面起動補助の初版でPythonにないSW_SHOW属性を参照して失敗。Win32の値5へ修正し、別run名で再実行した。
- ビルド時にpwsh.exe不在のメッセージあり。MSBuildは0終了し、実行検証も成功。
- computer-useで画面目視確認を試みたが、`Computer Use native pipe is unavailable ... (os error 2)`で接続失敗。再初期化でも回復しなかった。画面の文字切れ・配置・閉じる操作の目視検証は未完了と明記した。
- G0の撮影/復号時刻の誤対応は未修正。G1-03の必須修正として維持した。
- task/seedは現段階では識別ラベル。物理・映像・制御は未接続。全summaryのtask_resultはnot_run、closed_loop_validatedはfalse。

### 証拠と現在地

証拠は`artifacts/reach_g1_20260921/`の`build_Release*`、`tests_build_fixed.log`、`unit_test_result.txt`、`unit_sessions_final/`、`verification/integration_01/`、`verification/legacy_smoke_01/`、`runs/foundation_smoke_01/`、`runs/foundation_ui_02/`。同フォルダはgit対象外。

最終確認ではPython構文、プロジェクトXML、設定JSON、文書参照とコードフェンス、38タスクの一意性・完了8件を確認した。統合/既存モード試験のexeハッシュも現バイナリと一致。`git diff --check`はエラーなし。結果は`final_checks.json`に保存した。

G1-01を完了へ更新。G1は1/6項目完了、研究ゲートは引き続きG0のみ1/7。次はG1-02の世界・運動・真値評価器と、G1-03の撮影/符号化/復号対応。仕様はv0.1.2として基盤実装への参照と結果の意味を追記した。

## E006 — 2026-09-21：仮想ロボット接続と撮影/復号対応修正

対象ID：G1-02、G1-03。着手時は未着手。ユーザーの指示に従い、前進・制動で接続を検証する範囲とし、画像による自動制御はG1-04へ残した。

### 実装と意図

- `ReachRobotWorld.*`に100 Hzの運動、加減速、T1/T2の環境・真値評価器、透視投影カメラを追加。同じ初期状態からの再構築をリセットとし、固定刻みで軌跡を再現する。
- `ReachRobotVideo.*`に独立したencoderワーカーと、H.264/RNVP v3/localhost UDP/既存decoderの接続を追加。画像に番号を描き込み、受信画素との一致を確認する。
- `FrameIdentityLedger.h`とH264Encoderの追跡API、NetworkVideoReceiverのPTS照合を追加。受理した入力だけを台帳へ登録し、出力PTSで元の撮影情報へ戻す。現在入力の撮影情報による上書きを廃止する。
- 研究経路に欠番検出・flush・IDR待機、最大32 AUの順序付き受信キュー、4 MiB受信バッファ、selectによる待機を追加。既存モードのキュー設定・受信タイムアウトは従来のまま。
- encoder/decoderの終了時drain、形式変更時の正しい出力形式選択、NV12格納高の扱い、COM/MFのワーカー単位管理を追加。同期・非同期の待ちを観測値から消さない。
- Workbenchに受信映像・真値マップ・照合枚数・撮影→復号時間を追加。ユーザー画像で切れていた保存先を、横スクロール可能な読み取り専用欄へ変更した。
- 実行用robot JSON、起動cmd、単体テスト、実映像受入スクリプト、実装説明書を追加。既存のPython起動器はstage選択と実行ごとの診断ログ分離へ拡張した。

### 検証中に見つけた問題と処置

1. `robot_loss_01`：１ AU欠落後、復号APIは成功しても24枚の画像番号が読めなかった。これを成功にせず、研究経路に欠番後の参照破棄とIDR待機を追加した。
2. `acceptance_01`：非同期encoderで形式変更の直後に出力を再要求し、E_UNEXPECTEDを検出した。次のHaveOutputイベントまで待つよう修正した。
3. `acceptance_02` / `robot_decoder_init_01`：decoderの出力形式を手作りし直すと、起動時の再交渉に失敗して先頭44枚が出なかった。MFTの提示する実形式を使用し、格納高と表示高を区別した。`robot_decoder_init_02`は６枚全一致。
4. `acceptance_03`：0.1秒のsoftware試験で、末尾のまとまった出力を既存の最新優先キューが間引いた。研究経路を最大32 AUのFIFOに変更し、二つのスレッドによる取り出し順も保護した。
5. `acceptance_04/05`：意図的な損失を設定していないのに単一パケット欠番を観測した。バッファ拡大だけでは解消しなかった。`packet_audit_01`で送信247/受信246、seq 15の不着を送受信境界のログで確認。研究経路のSO_RCVTIMEOを無効にし、selectで到着を待つ方式へ変更した後、`packet_audit_02`は247/247、後述の最終試験も合格した。OS内部の喪失原因を断定したものではない。
6. パケット診断の初版でgetenvの非推奨をエラー扱いするビルド設定に抵触。GetEnvironmentVariableAへ変更した。pwsh.exe不在の既存メッセージは残るが、最終MSBuildは終了コード0。

途中の失敗を含む証拠は削除していない。最終合格は同一バイナリの`acceptance_06`で判定した。

### 最終結果

- Release x64ビルド成功。exe SHA-256：`c1a7a3f9fc28f1cd90a172601ea9661eed6693ebcb1795c6f352100a43db66d3`。
- 世界・台帳の単体280アサーション、基盤の単体663チェック、基盤実アプリ12ケースに合格。
- 映像受入６ケース：15秒autoで450枚、４秒softwareで120枚、初期状態変更/リセットの３秒autoを２回各90枚、0.1秒softwareで３枚、AU欠落後のIDR復帰で64枚。合計817枚すべてで画像番号・stream・撮影時刻・出力PTSが一致。意図しないパケット欠番とUDPキュー破棄は0。
- software試験は終了時に16枚、短いsoftware試験は全３枚をencoderから排出した。decoder末尾出力も検証に含めた。
- 既存モード８秒：終了0、記録対象の復号140件、表示95枚。性能比較ではない。
- 新画面`robot_ui_final_01`は15秒完走し、450枚すべて一致、UDP欠番0。完了画面をユーザーが閉じるまで残した。
- 保存BMPの受信画素を確認。Windows画面確認ツールは再試行しても`native pipe is unavailable (os error 2)`で失敗したため、新画面全体の文字切れ・配置・操作の目視検証は未完了。

### 証拠・現在地

`artifacts/reach_g1_robot_20260921/`の`build_Release*`、`unit_report.json`、`packet_audit_comparison.json`、`verification/acceptance_06/report.json`、`verification/foundation_regression_01/report.json`、`verification/legacy_regression_01/report.json`、`runs/robot_ui_final_01/`を参照する。詳細は`docs/Reach_RT_G1_Robot_Video.md`。

G1-02/03を完了とした。G1は3/6項目、全タスク10/38項目完了、研究ゲートはG0のみ1/7。specはv0.1.3。次はG1-04の画像認識・姿勢推定・固定制御器、続いて指令UDPと理想回線18初期姿勢の確認。自動作業成功と研究効果は未検証である。

## E007 — 2026-09-21：報告形式と説明方針の明文化

ユーザーから「毎回、現在の状況と次工程を明確にし、実装内容もプロの場で説明できるよう分かりやすく説明する」と指定された。

進捗管理資料の2章に、現在地→実装内容と目的→検証結果と限界→次工程の報告形式を追加した。各機能の入力・処理・出力、具体的な動作の変化、研究上の役割を説明し、次工程にはタスクIDと完了条件を添える。冒頭の仕様書バージョン表記を実際のv0.1.3へ修正した。

今回は報告方法の整備で、実装コード・実験結果・タスク完了数は変更していない。現在地はG1-01～03完了、G1は3/6項目、研究ゲートはG0のみ1/7。次工程はG1-04。真値を入力にせず、受信画素からの推定・指令生成・観測喪失時の扱いを確認する。

## E008 — 2026-09-21：G1-04 受信画像による認識・制御

対象ID：G1-04。未着手→作業中→検証→完了。ユーザーの指示に従い、実装の意図と前提、結果、現在地、次工程を会話・資料に記録した。

### 実装と意図

- `ReachVisualControl.h/.cpp`を追加。受信輝度画素と照合済みメタデータから黒枠・模様・四隅を検出し、既知平面と較正済みカメラから位置・yawを推定する。認識・制御APIは`RobotTruth`や初期姿勢を受け取らない。
- 複数候補・曖昧さ・短い辺・模様不一致・画像境界・不正な時刻・推定急変を拒否。撮影から200 msの期限を認識時と制御時の双方で確認する。
- OBSERVE/APPROACH/ALIGN/HOLDの固定制御器を追加。画像推定・既知目標からv/ωを作り、誤差余裕・壁余裕不足では停止する。100 msの絶対期限と根拠画像を記録する。
- `visual_control` stageと専用JSON・起動cmdを追加。G1-04では診断用のローカル適用で物理へ接続し、UDP受信器への置換をG1-05へ残した。既存の`robot_video`接続確認は別stageに維持した。
- `observations.csv`、`commands.csv`、`local_applied_commands.csv`、評価専用`capture_evaluation.csv`、モデル/要約を追加。画面に検出枠、画像推定、指令、使用画像と年齢、停止理由を追加した。

### 検証で見つけた問題と修正

1. `calibration_01.csv`：１画素１点の白黒描画では、54観測で最大位置誤差0.7605 m、最大yaw誤差0.3611 rad。小さい再投影誤差だけでは正しい位置を保証しないと確認した。
2. カメラを投影図形の画素被覆面積による描画へ変更し、認識側を境界輝度の積分へ変更。マーカー寸法・配置・物理・目標を変更せず、最終較正は最大位置誤差0.004692 m未満、yaw誤差0.001177 rad未満になった。途中の8×8サンプリング/補間版のCSVも保存した。
3. `acceptance_01`：T2の長めの実行で周期上限超過。無効試行として保存し、画素処理中の動的配列確保を固定配列へ変更した。最終描画時間の最大16.649 ms。周期の上限は緩めていない。OSの全スケジューリング要因まで原因を断定したものではない。
4. `acceptance_02`：T2で圧縮後の１推定が初期誤差余裕を逸脱（位置誤差約0.1163 m）。物理上限と固定の揺れ許容量で推定急変を拒否し、距離・再投影誤差に応じたINITIAL誤差余裕を追加した。推定不能を真値で補正せず停止へ渡す。
5. 停止・拒否時も画像年齢を正しく記録するよう修正し、最終版で再確認した。モデルJSONの式に含む文字とC++ raw文字列の区切りが衝突したビルドエラーは、名前付き区切りへ変更して解消した。

### 最終結果と証拠

- Release x64ビルド成功。exe SHA-256：`89afa9250587c7a103b3015cd7e2d1220f8aeac6f229fd734b41787b30d9f05e`。
- `verification/units_final_01/report.json`：認識・制御30、世界・台帳280、基盤663に合格。圧縮前54観測の較正も合格。これは18姿勢の閉ループ成功ではない。
- `verification/acceptance_final_01/report.json`：同じexeで６ケース合格。撮影1,140枚、復号1,114枚、撮影ID/時刻不一致0、認識採用981枚/拒否133枚。採用画像の最大位置誤差4.753 cm未満、方位誤差1.089度未満、記録した初期誤差余裕の逸脱0。全760指令の根拠画像・年齢・絶対期限・実適用を監査した。
- T2中央12秒で接近→向き合わせ→停止保持とローカル評価器の目標到達を確認。左右にずれた初期状態でも修正指令を生成した。AU欠落時は画像期限で停止し、IDR復帰後に再開。softwareでは遅延画像を拒否し、全指令が停止だった。
- `verification/foundation_regression_01/report.json`：同じexeの基盤実アプリ12ケース合格。
- 証拠の根は`artifacts/reach_g1_visual_20260921/`。失敗・中間版は削除していない。画面全体の目視確認は未完了。実機、未知マーカー、未知照明、他PC、指令UDP込みの作業成立や研究方式の優位性は未検証。

追加の確認として、同じexeの旧`robot_video` stageを３秒実行し90枚全一致、時間指定の指令源を維持していることを確認した。T2中央の保存済み復号BMPでは、床とマーカーの実画素を確認した。これは画面全体の目視検証ではない。`final_checks.json`に、現在ソースと最終試験のハッシュ一致、指令年齢、Python/XML/JSON/UTF-8文書の参照・フェンス、38タスクの一意性と完了11件、`git diff --check`合格を記録した。

詳細・再実行手順・専門の場での説明例は`docs/Reach_RT_G1_Visual_Control.md`。仕様はv0.1.4。G1-04完了、G1は4/6項目・全体11/38項目完了。研究ゲートはG0のみ1/7。次はG1-05の指令UDP・期限・ウォッチドッグ、その後G1-06の18初期姿勢での成立確認を行う。

## E009 — 2026-09-21：G1-05 指令UDP・期限・ウォッチドッグ・制動

対象ID：G1-05。未着手→作業中→検証→完了。ユーザー指定の重複・遅着・通信断を含め、受信後の実適用と速度変化まで確認した。

### 実装と意図

- `ReachCommandProtocol.h/.cpp`：固定88 byteのRCMD v1を追加。big endian、GUID 16 byte、sequence、生成/絶対期限/撮影時刻、根拠frame/stream、固定小数点速度、状態・理由、CRCを明示。構造体の生メモリ送信を避ける。
- `CommandGuard`：別セッション・重複・古い連番・未来・不正値・長さ・CRC等を拒否する。期限切れの新しい正常電文は連番上限のみ更新し、有効受信時計は更新しない。既存指令の期限が切れた場合と、有効受信が250 ms途絶えた場合を区別する。
- `ReachCommandUdp.h/.cpp`：別々のloopback送受信ソケット、予定したpeerの確認、非同期に待たない受信処理、固定診断用遅延/破棄を追加。生成した指令をシリアライズし、受信側で復元したものだけを物理へ渡す。
- `command_udp` stage・専用JSON・起動cmdを追加。G1-04の`visual_control` stageはローカル診断として残した。標準起動はG1-05へ更新。
- 物理側は毎刻み、受信側が選んだ速度目標だけを適用する。100 ms期限が先に制動を始め、250 msのwatchdogは通信断を引き続き記録する。並進0.60 m/s²・旋回1.6 rad/s²のモデルを維持し、瞬時停止へ置き換えない。
- 送信/受信の実バイト列、IP送信試行量、受理/拒否理由、最後の有効受信時刻、物理ごとの適用指令を追加。画面も生成指令と受信後の指令を分けて表示する。

### 検証と調整

`outage_smoke_01`で移動→期限切れ→watchdog→減速停止を確認。単体初版は、未来時刻のテストデータが同時に古すぎる根拠画像を持っていて送信APIが先に拒否したため、根拠時刻を修正して受信側の未来検査を独立に検証した。GUID表記に余分な区切りがある場合も拒否するよう厳密化した。最初の７ケース`acceptance_01`を保存し、最終バイナリで再実行した。

### 最終結果

- Release x64成功。最終exe SHA-256：`ed0a671706d0f8275d26f7a6cf4797c3c072f079b1556e6e814903d079945bdf`。
- `verification/acceptance_final_01/report.json`：通常・重複・順序逆転・150 ms遅着・通信断・復帰・不正電文の７ケース合格。700指令生成、680送信/680受信、意図しない指令不着0。映像1,050枚の撮影情報が一致し、3,500物理刻みの受信指令・期限・加減速を照合した。
- 重複60件、順序逆転１件、期限切れ76件、不正４件を拒否。期限切れや不正電文でwatchdogの時計が更新されないことも監査した。別セッション/未来の大きい連番の後にも正常指令を受理した。
- 通信断と復帰の両ケースで、制動開始前0.30 m/sから0.60 m/s²で減速し、0.50秒・0.075 mで停止。絶対期限から判定までの遅れは5.288/11.613 ms、最後の有効受信から停止記録までは603.117/597.428 ms。シミュレーション刻みと実時計を分けて記録した。断中の再移動はなく、復帰後は新しい有効指令で再開した。
- 通常12秒のT2中央条件で、指令UDPを経由した目標到達を確認。18初期姿勢の安定性を保証する結果ではない。
- 指令経路のIP層送信試行量78,842 byte。送信前破棄80指令と終了時保留４指令を送信済みに数えない。全方向・全補助通信を含む研究用予算の完成はG2。
- `verification/units_final_01/report.json`：指令178、認識30、世界・台帳280、基盤663に合格。指令178のうち142は71刻みの並進/旋回減速検査である。
- `verification/foundation_regression_01/report.json`：同じexeの基盤12ケース合格。`runs/visual_regression_01`では旧visual stageを３秒実行し、90画像照合・60ローカル指令を確認した。

証拠の根は`artifacts/reach_g1_command_20260921/`。詳細・通信形式・前提・再現手順・専門の場での説明例は`docs/Reach_RT_G1_Command_UDP.md`。画面全体の目視確認は未完了。固定診断は容量・ジッタ付き回線モデルではなく、実機安全保証・複数PCの時計同期・研究方式間の性能比較は今回の対象外。

仕様をv0.1.5へ更新。G1-05完了、G1は5/6項目、全タスク12/38項目完了。研究ゲートはG0のみ1/7。次はG1-06でT1/T2の18初期姿勢を実行し、真値評価器による作業成立と失敗条件を確認する。

## E010 — 2026-09-21：G1-06 18初期姿勢の検証とG1保留判定

対象ID：G1-06。未着手→作業中→18条件検証実施→阻害要因あり。**G1ゲートは未合格（保留）**。検証の実施とゲート通過を区別する。

### 実装と意図

- `verify_reach_closed_loop.py`を追加。T1/T2の各9姿勢を事前計画へ保存し、実映像・受信画素認識・指令UDPで各60秒実行する。実験本体はC++、Pythonは実行管理と保存記録の独立監査を担う。
- 指令バイト列の監査に加え、受信指令から加減速・位置・向きを再計算し、成功時に速度を固定する前の値で1秒保持を検証する。成功フラグや終了コードだけを成功証拠にしない。
- 18姿勢・目標・物理・60秒窓は維持。T1の停止準備距離を8 cmから2.5 cm（解除4 cm）へ修正し、出口手前で停止する不整合を解消。T2は8/10 cmを維持した。T1の誤差・年齢・制動移動を差し引いた後の壁余裕追加分を5 cmから2 cmへ共通変更し、G0のINITIAL値との差を仕様v0.1.6へ明記した。安全保証とは扱わない。
- `audit_reach_closed_loop_evidence.py`、`test_reach_closed_loop_audit.py`、`report_reach_closed_loop.py`を追加。原記録監査、不正記録拒否検査、実測軌跡と全件表のHTML/SVGを用意した。既定の画面起動cmdは今回のビルド・60秒窓を使う。

### 失敗と中間結果

1. 旧版の `g106_baseline_t1` は映像1,800枚・指令1,200件が届いたが、x=3.17476092471 mで停止し時間切れ。停止準備距離がT1出口と整合しなかった。
2. `matrix_01` はT1 y=−0.05 m/yaw−5度で壁余裕停止が持続し、約56.53秒で中断。途中CSVと理由を保存し、完走に数えない。
3. 壁余裕変更後の `matrix_02` は同姿勢で撮影キュー超過。物理13.47秒、最大encoder出力間隔879.491 ms。無効試行として保存した。
4. 固定版 `matrix_03` のT1 y=0/yaw−5度は18.19秒に世界ログ上の成功を記録したが、物理35.04秒で撮影キュー超過。最大encoder出力間隔923.034 ms、終了付近に約704秒の時刻差。60秒窓が未完了なので無効。OS/電源・MFT/ドライバ・待ち処理のどれが根因かは未確定。
5. 有効試行でも採用推定3枚が誤差余裕を逸脱。最大位置誤差0.122809 mに対し当該余裕0.100986 m。うち指令に使われた1枚では前進停止、残る2枚を根拠とする指令はなかった。誤差モデルの成立保証にはしない。

### 最終結果・証拠・判定

証拠の根は `artifacts/reach_g1_closed_loop_20260921/`。Release exe SHA-256：`bc8ce6bc1eb099380abfa3d78bac69e1cbcb3045405f18909017e502302473d5`。

- `verification/matrix_03/report.json`：自動再試行なしで18条件実施。T1は8合格・1無効、T2は9合格。全18試行の設定・nativeソース・exe一致を確認。有効成功はT1 15.93～34.13秒、T2 10.21～10.57秒。
- 有効17件の30,600画像・20,400受信指令・102,000物理刻みを監査。認識採用30,436/拒否164。接触・場外・時間切れ0。T1実測最小壁距離0.124512 m。採用推定の最大yaw誤差1.76031度。
- `verification/units_02/report.json`：指令178、認識・制御34、世界280、基盤663と、圧縮前較正54観測が合格。
- `auditor_fault_tests.json`：正常記録と8種類の改変記録の拒否を確認。一時フォルダーの権限制約で実行が失敗したため、許可後に検査を完了。試験コピーを証拠フォルダー内へ置く変更後も同じ制約が発生したが、許可後の最終再検査は合格した。
- `verification/command_regression_01/report.json`：同じexeでG1-05の7ケース再検査に合格。通信断の制動は0.30 m/sから0.50秒・0.075 m。
- `matrix_03/index.html`と2枚のSVGへ全件結果・軌跡を出力。`gate_decision.json`にG1保留と理由を保存。HTMLとアプリ画面全体の目視検証は未実施。

前提・再実行・専門の場での説明例は `docs/Reach_RT_G1_Closed_Loop.md`。失敗を削除せず、有効17件だけで「成功率100%」とは主張しない。1姿勢1回の結果を成功確率や他PCでの保証と扱わない。

G1-06は完了条件未達。G1は5/6項目、全タスク12/38項目、研究ゲートG0のみ1/7のまま。次はG1-06を継続し、映像処理の停滞・終了遅延と認識誤差余裕を改善して全18姿勢を再検証する。G1通過後にG2-01の双方向容量・直列化・有限キューへ進む。

## E011 — 2026-09-21～22：G1-06 処理停滞・推定誤差余裕の改善と18条件再検証

対象ID：G1-06。阻害要因あり→作業中→再検証完了→**完了、G1ゲート通過**。

### 実装と意図

- `ReachRobotVideo.cpp`で画素変換・認識・ログI/Oを共有ロックから分離し、撮影時刻検索と最新観測の公開だけを短いロックで行う。遅い認識が撮影・制御側を待たせる構造を改めた。
- `AsyncInputCredits.h`とH264Encoderの追跡経路で非同期MFTの入力要求を回数管理し、イベントを最大16件まとめて処理する。複数要求を真偽値で失わない。実際の本PC試行では未消費要求の最大は1だったため、これだけが旧キュー超過の根因とは断定しない。
- 研究用H.264観測通知を表示用鮮度破棄から分離した。認識・制御の撮影年齢200 msは維持し、古い画像を制御へ通さない。
- `ReachBufferedLog.h`による各4 MiBの上限付きバッファ、段階別CSV、終了段階JSON、キュー超過時の段階記録を追加。実行器に絶対期限の子プロセス監視を追加した。ドライバ内部の停止を強制解除できる保証ではない。
- `VisualHistoryGate`で過去500 ms・最大16推定と横方向の到達可能範囲を照合する。位置誤差余裕の固定項を2 cmから2.5 cmへ変更。壁方向余裕には経時移動をyaw余裕・角速度込みで投影する。真値入力・18姿勢・成功条件・60秒窓・物理・キュー上限・追加壁余裕2 cmは維持した。
- `verify_reach_closed_loop.py`に全採用画像の誤差余裕検査を追加。`verify_reach_stability.py`で250 msの認識停滞とsoftware遅延を検証。過去画像の再評価をC++単体へ追加した。実験本体はC++、Pythonは管理・監査・結果生成を担う。

### 失敗と予備試行

- `pipeline_01`は120 ms遅延で120復号出力に対し119観測となり不合格。表示鮮度によるframe 61の破棄を特定し、研究観測経路を修正した。`pipeline_02`および最終版`pipeline_final_01`の250 ms診断は合格。
- `pilot_t1_01`は誤差余裕変更後、壁余裕停止でx≈0.00156 mのまま60秒時間切れ。経時移動を横方向へ投影する修正後の`pilot_t1_02`は同じy=+0.05 m/yaw=+5度で19.54秒成功、全60秒完走、余裕超過0。
- 失敗・予備試行は保持し、18条件の本試験には混ぜない。過去30,436採用推定の履歴再評価は29,799受理・637拒否。旧余裕超過3枚はいずれも履歴検査で拒否された。

### 最終検証・成果物

証拠の根：`artifacts/reach_g1_stable_20260921/`。最終Release exe SHA-256：`6100e30bef812134b86f779c9b80e5e1e230d8ca4e4629c52689d609b5f56160`。

- `verification/matrix_final_01/report.json`：自動再試行なし、同一exe・nativeコード、T1/T2各9条件が全て合格。各60秒。T1 16.99～24.14秒、T2 10.26～11.47秒で成功。
- 32,400画像・21,600指令・108,000物理刻みを監査。採用31,802・拒否598、採用画像の余裕超過0。最大位置誤差0.080470 m、最大yaw誤差1.151334度。最小壁距離0.124758 m。接触・場外・時間切れ・無効0。
- `pipeline_audit.json`：撮影キュー最大1/上限8、待ち時間最大22.863 ms、入力処理最大15.007 ms、認識最大7.071 ms、観測公開ロック待ち最大0.303 ms、復号時画像年齢最大115.751 ms。encoder出力間隔最大79.902 ms。encoder終了待ち最大63.088 ms、decoder/UDP停止最大381.839 ms。
- `units_final_01`：指令178、世界280、基盤663、認識等30,515検査（過去CSV30,436行の形式確認を含む）が合格。非圧縮54画像の較正も合格。
- `pipeline_final_01`：250 ms停滞でも400物理刻み・120画像・80指令が揃い、期限切れ停止と新画像での再開を確認。software遅延時に移動しないことも確認。
- `command_regression_final_01`：7ケース合格。通信断で0.30 m/sから0.50秒・0.075 m制動。`foundation_regression_final_01`12ケース、`legacy_regression_final_01`既存復号/表示、`loss_idr_final_01.json`欠損時停止・IDR復帰も同一exeで合格。
- `evidence_audit.json`で全18条件のnativeソース・条件・全画像のID/撮影時刻/PTS一致を監査。`auditor_fault_tests.json`で8種類の不正記録、`allowance_fault_test.json`で過小な誤差余裕の拒否を確認。原記録は変更していない。
- `index.html`/SVGへ全結果・実測軌跡、`gate_decision.json`へG1通過を保存。仕様v0.1.7、進捗表、README、既定画面起動cmdを最終版へ更新。実装意図・処理構造・専門の場での説明例は`docs/Reach_RT_G1_Stability.md`。

### 現在地・限界・次工程

**G1-06完了、G1 6/6項目、全タスク13/38項目、研究ゲートG0/G1の2/7完了。** 次はG2-01：双方向容量・直列化・有限キューを実装し、既知サイズ・送信中の容量変化・キュー溢れを期待値と照合する。G2は今回未着手。

各姿勢1回の開発条件検証であり、G5保留テスト・成功確率・研究方式比較・他PC・実機安全性の評価ではない。旧停滞の唯一の原因やOS/ドライバ停止の完全排除を主張しない。アプリ画面全体と結果HTMLの目視確認は未実施。

## E012 — 2026-09-22：G2-01 双方向容量・直列化・有限キュー

対象ID：G2-01。未着手→作業中→実装・検証→**完了**。G2全体のゲートは未通過。

### 実装と意図

- `ReachLinkModel.h`にOS非依存のFIFOを追加。UDPペイロード+28 byteをIP長として扱い、送信中を含む占有でtail-dropする。送信途中の容量変化を未処理仕事量へ反映し、容量0では停止、再開後に継続する。完了は1 µs単位で切り上げ、空いていた時間を送信権として貯めない。
- `ReachDatagramLink.*`で方向ごとの実UDP中継・専用workerを実装。上り映像と下りRCMDに独立した容量・キューを持たせ、同じ実験原点を使う。入口と送信ソケットを分けて応答の反射を防止。モデル完了時刻、実sendto時刻、占有、tail-drop、終了時残留、電文を保存する。
- `ReachFoundation.*`に任意の`link_model`設定を追加し、未知キー・型・範囲・時刻順を検査。設定を省略したG1経路は維持。`ReachRobotVideo.*`と`ReachCommandUdp.*`へ中継を接続し、G2で意図した損失とG1の完全配送を区別する。指令期限・監視・物理・画像認識は変更しない。
- `ReachResearchMode.cpp`へG2モード名と現在の上下容量表示を追加。`run_reach_g1.py`に`--link-config`、既定条件JSON、`open_reach_g2.cmd`を追加した。
- `verify_reach_link.py`で容量積分・queue判断・保存則と、元送信→中継入口→中継出口→最終受信を独立監査する。指令監査はG2だけ、モデルで説明できる損失を照合する明示オプションを追加し、従来G1の無損失要件は維持した。
- `verify_reach_link_config.py`、`test_reach_link.cpp`、`test_reach_link_audit.py`、`report_reach_link.py`を追加。実験本体とモデルはC++、Pythonは管理・監査・結果生成を担う。

### 失敗と修正

- `units_01`はPath/PATH環境変数の重複によりMSBuild起動処理で失敗。環境辞書を正規化した`units_02`で回線29検査が合格。
- `acceptance_01`は同一中継ソケットへ戻った未利用RNVP応答を映像方向へ折り返し、送信元とモデル入口が不一致となった。監査が拒否し、入口/出口を別ソケットに修正。`acceptance_02`の5ケースは合格。
- `acceptance_final_01`の指令停止ケースは、停止前速度が最大0.228 m/sで前提の0.25 m/s超に届かず不合格。回線の計算は一致した。速度条件を緩めず、停止を1.5～2.2秒から3～4秒へ移し、同ケースの観測窓を4秒から6秒へ変更した。他の4ケースは4秒。失敗結果を保持した。

### 最終結果と証拠

証拠の根：`artifacts/reach_g2_link_20260922/`。最終exe SHA-256：`d24be093df38d62e95cfdd66cdcc079db6f3ebe6f458e02a468175cd681c62bb`。

- `verification/acceptance_final_02/report.json`：広い回線、映像停止、指令停止、映像低容量、指令送信途中の容量変更の5/5合格。合計22秒・660撮影・440指令・2,200物理刻み。上り3,077・下り440パケットを照合。復号462画像の番号・撮影対応が一致。
- 上りtail-drop591、下り88。終了残留は映像低容量1・指令途中変更2パケットを別記録。意図した通信不能の復号0枚を作業成功と扱わない。
- 指令容量停止で0.30 m/sから0.50秒・0.075 m制動し、新指令で復帰。指令途中変更では実パケットが変更時刻をまたぎ、予定完了時刻が容量積分と一致。遅着指令は拒否され、移動しなかった。
- OS送信実行遅れの最大は15.214 ms。モデル予定時刻と実送信を分けて記録した値であり、1 µs精度の実回線を実現したという主張ではない。
- `units_final_01`：回線29、指令178、認識・制御等76、世界280、基盤663が合格。`config_final_01`の不正回線設定12ケースは実アプリが全て拒否。
- `auditor_fault_tests.json`：早すぎる完了・誤った開始・占有量・tail-dropの4改変を拒否。`evidence_audit.json`：315個のnativeソースハッシュ照合、exe・宣言設定・方向別容量表の一致。
- `command_regression_final_01`の7ケース、`foundation_regression_final_01`12ケース、`legacy_regression_final_01`が同一exeで合格。容量モデルなしの`g1_t1_regression_final_01.json`は17.04秒成功、60秒の1,800画像・1,200指令・6,000物理刻みを監査し余裕超過0。今回の新exeで全18姿勢を再実行したとはしない。
- `index.html`へ全件表と証拠リンク、`task_decision.json`へG2-01完了・G2ゲート未通過を保存。仕様v0.1.8、進捗表、README、[G2説明書](Reach_RT_G2_Link.md)に意図・構造・定義・前提・再現手順・専門の場での説明例を反映した。

### 現在地・制限・次工程

**G2-01完了、G2は1/6項目、全体14/38項目。研究ゲートはG0/G1の2/7。** 次はG2-02「時刻基準の障害トレース・乱数分離」。送信数・順序が異なる場合も外生的な障害時間帯を揃え、方向・現象ごとの乱数系列を分離する。

今回の下り接続はRCMD。未利用RNVP応答はG1同様に制御へ使わず、中継へ反射させない。全ACK・状態通知を含む共通IP予算、ランダム損失・ジッタ、復号状態通知は後続工程。終了時は方向ごと最大250 msの実時間drainを行い、残留を記録する。モデルの合格と作業の成功・研究方式の優位性は別。画面全体とHTMLの目視確認は未実施。

## E013 — 2026-09-22：G2-02 時刻基準の障害トレース・乱数分離

対象ID：G2-02。未着手→作業中→実装・検証→**完了**。G2全体は未通過。

### 意図・実装・前提

- 同じseedでもパケット数で乱数の消費位置が変わる問題を防ぐため、パケット情報を入力に持たない事前生成器を追加。名前付きSHA-256キーと時間セル番号で上り／下り×容量／損失／ジッタを分離した。シーン・認識・探索の系列は予約のみで、現在の決定的処理は維持。
- `LinkConfig`に時刻区間ごとの損失・遅延を追加。`PropagationModel`が直列化完了のモデル時刻で状態を選び、送達予定時刻順に実UDPへ配送。可変遅延による順序逆転、同予定時刻のID順、時刻損失とFIFO破棄の分離、伝搬中の終了残留を実装した。
- `ReachDatagramLink`は方向別の原トレース・SHA-256、適用区間・遅延・予定時刻・実送信を記録。OS処理の遅れを合成ジッタへ混ぜない。伝搬待ち16 MiB／131,072個の上限超過は試行エラーとする。
- `run_reach_g1.py --trace-bundle`は生成結果・期間・内容を照合し、固定表と生成manifestを試行へコピーする。長い表に対応するためJSON入力上限を4 MiB、容量・障害表を各4,096点とした。旧設定の障害表省略は損失なし・遅延0。
- `config/reach_rt_g2_trace`へseed 201・60秒・100 msセルの固定デモを生成。`open_reach_g2_trace.cmd`と現在状態の画面表示を追加。旧G1/G2-01起動は各固定版のまま。
- 固定するものは容量・損失時間帯・遅延。キュー滞留や実際の損失数は方式の送信内容・時刻に応じて変わる。今回の損失は時間セルのブラックアウトで、パケット独立損失ではない。

### 実行と検証結果

ビルド：`powershell -NoProfile -ExecutionPolicy Bypass -File tools/build_reach_g0.ps1 -Configuration Release -OutputName reach_g2_trace_20260922`。Release x64成功。SDK由来の警告と既存post-buildの`pwsh.exe`未検出表示あり、ビルド終了コード0。

固定exe SHA-256：`b79b26732444db4b2a956034967cfa41f2c81f54d1f949be65ad3442df6083f0`。

- `verify_reach_trace.py --name acceptance_01 --build-name reach_g2_trace_20260922`：5/5合格、3,737パケットを監査。T1上り917個・時刻損失136、T2上り900個・時刻損失155でも上下のトレースハッシュは一致。T1/T2は送信列を変える診断用で、性能比較ではない。
- 指令の3～4秒損失区間では20個を時刻損失として分類。0.30 m/sから0.50秒・7.5 cmで停止、新指令で復帰。下り250 ms→0の遅延短縮で順序逆転・期限切れ拒否を確認。下り1秒遅延の0.5秒試行では伝搬中10個を終了時残留に分類。
- `units_01`：C++回線124、指令178、認識等76、世界280、基盤664が合格。回線検査には40/400個の異なる入力数、境界時刻、照会順序の独立性を含む。
- `generator_01`：48検査合格。再生成一致、方向／現象／予約系列の独立性、600/6,000回照会、期間延長、固定表・seedの改変拒否等。
- `config_01`：不正設定22ケースを実exeがセッション生成前に拒否。`trace_auditor_faults.json`：監査器が6種類の原記録のメモリ内改変を拒否。
- `link_regression_01`：G2-01の5ケース合格。`foundation_regression_01`：基盤12ケース合格。`long_trace_smoke_01.json`：97,273 byteの有効設定で1秒を実行・監査。60秒の試行完走を意味しない。
- `evidence_audit.json`：nativeソース315ハッシュ、exe・要求設定・有効設定・方向別表の対応を確認。最大OS実行遅れ16.549 ms。
- [説明書](Reach_RT_G2_Trace.md)、[結果一覧](../artifacts/reach_g2_trace_20260922/verification/acceptance_01/index.html)、仕様v0.1.9、進捗表、READMEへ意図・構造・結果・限界と専門の場での説明例を反映した。

### 現在地・制限・次工程

**G2-02完了、G2は2/6項目、全体15/38項目。研究ゲートはG0/G1の2/7。** 次はG2-03「IPバイト計上・共通予算」。ACK・FEC・再送・指令等の内訳／総量を照合し、方式間で同じ予算を守らせる。

実5Gの測定分布、独立した複製設定、全ACK・状態通知、研究方式の優位性は今回の確認範囲に含めない。今回の新exeでG1全18姿勢・各60秒を再実行していない。アプリ全体・HTML全体の目視確認は未実施。失敗を除外して再試行した実通信ケースはない。期待した不正設定・監査改変の拒否は検証結果として保持。

## E014 — 2026-09-22：G2-03 IPバイト計上・共通予算

対象ID：G2-03。未着手→作業中→実装・検証→**完了**。G2全体は未通過。

### 意図・実装

- 映像だけのビットレート比較ではFEC・再送・応答の追加費用が隠れるため、実sendto直前に共有のIP予算ゲートを設けた。IPv4 20 byte＋UDP 8 byte＋データグラム全体を費用とし、全体／方向別のtoken bucket・累積上限を同時に検査する。
- `ReachIpBudget`、`ReachBudgetTransport`、`DatagramSendHook`を追加。元映像・IDR・FEC・再送・指令と、UdpReceiverの4つの応答送信箇所を接続。送信前棄却は送信試行量へ加えず、送信後の損失には返金しない。要求が入った順に許可し、不足時は待機ではなく棄却する基盤である。
- RNVP応答を指令と同じ下り容量FIFO・障害表へ通し、配送後に宛先を分けた。既存のACK起因再送を有効にして課金する。FEC群サイズを設定化し、全フレーム再送のDataに再送フラグが付かない経路も修正。
- 新規送信を観測期間末で止め、既送信分のdrain、応答受信確認、ログ確定の順序を追加。ボトルネックの部分サービスを整数bit×µs単位で記録する。
- 方向／種別／判定／生電文／累積費用の台帳と、全イベントを独立再計算するPython監査を実装。予算指定を省略した旧研究経路を維持した。

### 途中の失敗と修正

- `units_01`はテスト電文の必須ヘッダ未設定により分類検査で失敗。正しいRNVP電文へ修正した。検査基準は緩和していない。
- `acceptance_01`は最後の損失復帰試験で、下り中継が応答190個を送出したのに送信側受信が189個となり無効。元のログ・ソース記録を保持。
- 単発の応答欠落の根本原因は特定できていない。研究モードの応答受信を非ブロッキング＋1 ms待機・4 MiB受信バッファへ変更し、主要試験と同条件の追加2試行で全電文一致を確認。今後もモデル外の欠落を合成損失へ混ぜず無効試行として扱う。

### 実行と検証結果

Release x64ビルド成功：`tools/build_reach_g0.ps1 -Configuration Release -OutputName reach_g2_budget_20260922`。最終exe SHA-256：`62ad0ed68c68bdec31c00f05400f4fdff59e3d98cad82ad78c07d031976bbb5b`。SDK警告・既存post-buildのpwsh未検出表示はあるが終了コード0。

- `verify_reach_budget.py --name acceptance_02 --build-name reach_g2_budget_20260922`：5/5合格。5,853件の台帳イベント・3,380,007 IP byteを監査。
- 全種別6秒の送信試行は上り1,369,680＋下り48,368＝1,418,048 byte。実映像・IDR・FEC・選択再送・ACK/NACK・制御応答・受信状況通知・指令の内訳と一致。
- 共通上限10,000 byteでは9,925 byteで停止、レート128 kbps／4 KiBバーストでも全時点の制約を確認。下り累積26,000 byteでは25,936 byteで止まり、ロボットは0.30 m/sから0.50秒・0.075 mで制動。上下の時刻損失費用が課金されたままであることも確認。
- `loss_repeat_01`：修正後の損失復帰条件を追加2回実行し、各190個の応答受信と配送電文が一致。
- `units_final_01`：回線・予算145、指令178、認識等76、世界280、基盤664が合格。`config_final_01`：実exeで不正予算設定17ケースを拒否。
- `auditor_fault_tests.json`：IPヘッダ除外、FEC誤分類、累積改変、棄却課金、ゲート迂回、応答欠落の6改変を検出。元記録は変更していない。
- `trace_regression_01`：G2-02の5ケース合格。`command_regression_01`：指令7ケース合格。`foundation_regression_01`：基盤12ケース合格。`legacy_regression_01`：旧RNVPモードの起動・復号・表示を確認（性能比較ではない）。
- `evidence_audit.json`：nativeソース335ハッシュ照合、exeと要求／有効予算、制動計算を確認。最大OS配送実行遅れ17.392 ms。
- [実装説明](Reach_RT_G2_Budget.md)、[結果HTML](../artifacts/reach_g2_budget_20260922/verification/acceptance_02/index.html)、仕様v0.1.10、進捗表、README、G2-03起動用cmdを更新。

### 現在地・制限・次工程

**G2-03完了、G2は3/6項目、全体16/38項目。研究ゲートはG0/G1の2/7。** 次はG2-04「復号状態・参照世代・期限の追跡」。パケットが揃ったことと利用可能な復号画像を区別し、IDR/P欠損・遅着・再同期を実デコーダで確認する。

G2-05の新規状態通知、最適な予算配分・優先制御、実回線の物理帯域測定、研究方式の性能優位性は未実装・未検証。今回の新exeでG1全18姿勢を再実行していない。アプリ／HTML全体の目視確認も未実施。旧AU提出数などをIP送信量へ読み替えず、IP台帳を正本とする。

## E015 — 2026-09-22：G2-04 復号状態・参照世代・期限の追跡

対象ID：G2-04。着手時：G2-03完了、G2は3/6・全体16/38。目的は再構成成功と実際に利用できる画像の成功を分離し、欠損・遅着・再同期を実デコーダの事実として残すこと。

実装意図・構造：[G2-04説明書](Reach_RT_G2_Decode.md)。`FrameReassembler`／`UdpReceiver`の局所観測通知、`NetworkVideoReceiver`の入力受理・実画素出力・flush記録、`FrameIdentityLedger`の実IDR世代・受理時刻引き継ぎを追加。`ReachDecodeJournal`が別mutexで台帳化し、表示・認識・制御採否を記録する。送信側の意思決定へ直結する状態参照APIは作らない。

表示採用には撮影＋200 msを適用し、表示中の画像IDと最後の復号IDを分離。観測IDが指令にコピーされただけでは制御利用と数えず、共通制御器の検査を通過した推定の利用を明示する。再構成器の実回復期限・未回復チャンク・欠番flush・再投入不可を記録。RCMD電文、回線・予算、制動条件、タスク成功閾値は変更していない。

最終コード：`artifacts/reach_g2_decode_20260922/bin/Release/GE3.exe`、Release x64、v145／SDK 10.0.26100.0。SHA-256 `4c35906132a8b0d3fa42680e81572b57a1c0d34c2bb6c29bb2b8a07ede0598a5`。ビルド終了0。非致命的な既存SDK警告・postbuildのpwsh不在表示は保持。

実行・証拠：

- `tools/verify_reach_decode.py --name acceptance_final_02`：[実通信8ケース](../artifacts/reach_g2_decode_20260922/verification/acceptance_final_02/report.json)。各4秒、T2、640×360／30 fps、MF／UDP。再構成836、入力受理664、実画素662、出力前破棄2、表示採用535、認識採用517、制御利用338回。入力受理＝画素出力＋破棄、出力PTS・画像内ID、制御根拠・各期限を独立照合。
- 初期IDR欠損は119再構成／76画像、参照P欠損は119／94。欠損AUのNALとsliceを独立解析し、IDRはtype 5・slice type 7、Pはtype 1・slice type 5・nal_ref_idc 2を確認。完全な参照グラフまでは検証しない。
- 認識300 ms停滞では表示棄却7件、遅いAUの順序どおりの入力受理後、同じ世代の新しい画像の表示採用が復帰。250 ms通信遅延は再構成期限でIDRを失い、23再構成／0画像。ソフトウェアエンコーダは120画像出力／0表示・認識・制御採用。いずれも「制御成功」に読み替えない。
- `tools/test_reach_decode_audit.py`：世代・時刻・状態・期限・制御利用・破棄・欠損数・IDRの8改変を全拒否。
- `units_01`：回線／予算145、指令178、画像制御81、世界／PTS282、基盤664が合格。世代をまたぐ遅延出力、200 ms境界、期限超過source IDを利用と数えないことを含む。
- 同じ最終exeで`command_regression_01`の7ケース、`budget_regression_final_01`の5ケース、`legacy_regression_01`を確認し、最終`task_decision.json`へ集約。

途中の不合格・変更：`acceptance_01`は「250 ms通信遅延でも多数の実画像が出る」という検証前提が不成立。既存再構成回復期限によるIDR消失を確認したため、期限を緩めず、通信期限切れとCPU処理停滞を別ケースにした。`acceptance_02`は8/8、欠損AU保存追加後の`acceptance_final_01`も8/8。表示ID整合修正後の最終`acceptance_final_02`を正本とする。途中版・初回予算回帰`budget_regression_01`は保存し、最終exeの証拠へ流用しない。

状態変更：G2-04完了。G2は4/6、全体17/38、研究ゲートはG0・G1の2/7で変わらない。仕様v0.1.11へ更新。仕様8節の計測条件と実装限界を明文化した。次はG2-05：状態通知を共通予算と逆方向回線へ通し、未到着状態を送信側が参照しない推定を実装・検証する。

未確認：G1全18姿勢は今回再実行していない。完全参照グラフ、未報告のデコーダ隠蔽、巻き戻し再投入、G2-05以降、研究方式の性能優位性、画面全体の目視確認は未検証。既存のG1／G2各固定版と証拠を保持する。

## E016 — 2026-09-22：G2-05 受信・作業状態の通知と送信側推定

対象ID：G2-05。着手時：G2-04完了、G2は4/6・全体17/38。目的は受信側だけが知る復号・認識・作業状態を逆方向回線へ通し、送信側が未到着の状態を利用せずに判断する基盤を作ること。

実装意図・構造・固定電文仕様：[G2-05説明書](Reach_RT_G2_Feedback.md)。`ReachDecodeJournal`で受信側の局所状態を一貫したスナップショットへまとめ、`ReachStateProtocol`でRSTA v1の208 byte電文へ符号化する。`ReachStateFeedbackUdp`が50 ms周期で、ACK・指令と同じ共通IP予算と下りFIFOを経由して送る。通常236 IP byteを計上する。受信側の画像由来の推定値だけを通知し、仮想世界の真値は入れない。送信側推定器は実際に受け取った電文、ローカル時刻、事前共有条件だけを入力とする。

通知生成からの古さと画像撮影からの古さを分離し、通知は250 ms以上で失効、観測は200 ms以下を利用候補とする。通信が途絶える間は既知の速度上限に応じて位置・方位の誤差余裕を広げる。重複・順序逆転・期限切れ・別session・CRC破損・不正長・未知version・未来時刻を扱い、古い通知で状態を若返らせない。判断は入力状態の利用可否であり、G4の再送・FEC・IDR選択は今回実装しない。

`ReachDatagramLink`に状態通知の逆方向転送先を追加し、`ReachIpBudget`へRSTA分類、`ReachResearchMode`へ受信・推定・表示と終了時の配送照合を接続した。設定は`state_feedback: true`で有効にし、模擬回線・共通予算を必須とする。省略時の既存経路は維持する。`verify_reach_state.py`と共通予算監査で、電文・UDP実受信・受信側イベント・認識・指令・推定を独立照合する。`finalize_reach_state.py`が固定証拠を集約する。

最終exe：`artifacts/reach_g2_feedback_20260922/bin/Release/GE3.exe`。SHA-256：`19a9bd19919d5a0b0a6dbd81e3e25004a913d37aa76f7bc4387165dd908d555d`。

検証結果：

- [実通信8ケース](../artifacts/reach_g2_feedback_20260922/verification/acceptance_01/index.html)が8/8合格。正常、通信断、順序逆転、全通知期限切れ、重複、5種類の不正電文、予算切れ、通知全損失を各4秒で実行。640通知スナップショット・640推定を監査し、通知送信試行量156,104 IP byteを照合した。
- 通知全損失では受信側で118画像が復号されても、送信側の80推定はすべて通知未到着。通信断では24推定で通知失効、回復後は新しく届いた通知へ更新。順序逆転3件・重複79件を棄却し、300 ms遅延では79電文すべてを期限切れとして扱った。
- [単体](../artifacts/reach_g2_feedback_20260922/verification/units_final_01/report.json)：状態通知83、回線／予算145、指令178、画像制御81、世界／PTS282、基盤／設定671が合格。[監査器への8改変](../artifacts/reach_g2_feedback_20260922/verification/acceptance_01/auditor_fault_tests.json)もすべて検出。
- 同じ最終exeで[G2-04の8条件](../artifacts/reach_g2_feedback_20260922/verification/decode_regression_01/report.json)が8/8合格。[既存RNVPモード](../artifacts/reach_g2_feedback_20260922/verification/legacy_regression_01/report.json)は8秒・準備2秒のスモーク確認で復号サンプル145行・表示97フレームを確認した。
- [証拠監査](../artifacts/reach_g2_feedback_20260922/verification/acceptance_01/evidence_audit.json)でexeと保存ソースを照合し、ネイティブソースのハッシュ照合1,152件が一致。[完了判定](../artifacts/reach_g2_feedback_20260922/verification/acceptance_01/task_decision.json)へ反映した。

状態変更：**G2-05完了、G2は5/6、全体18/38。研究ゲートはG0・G1の2/7。** 仕様v0.1.12、進捗表、README、説明書、[起動ファイル](../tools/open_reach_g2_feedback.cmd)を更新。次はG2-06「障害条件での統合計測」。障害→復号→認識→指令→実動作・作業結果を同じ時間軸で照合し、全体の因果性と通信量を確認してG2通過を判定する。

限界：G1全18姿勢・60秒を新exeで再実行していない。G4送信方策、研究方式の性能優位性、完全参照グラフ、別PC間時刻同期、実機・商用網の保証は未検証。アプリ／HTML全体の目視確認は未実施。誤差余裕は既知速度上限に基づく範囲であり、確率的な信頼区間ではない。過去の固定ビルドと証拠を保持する。

## E017 — 2026-09-22：G2-06 障害条件での統合計測・G2通過判定

対象ID：G2-06。着手時：G2-05完了、G2は5/6・全体18/38・通過ゲート2/7。依頼は障害→復号→認識→指令→実動作・作業結果を照合し、G2全体を判定すること。

実装意図・構造：[G2統合計測説明](Reach_RT_G2_Integration.md)。Pythonの`verify_reach_integration.py`で各段階をframe ID、指令sequence、physics tick、単調時計で結合。画像の認識値から固定制御器の指令を再計算し、実受信指令から加減速・旋回・接触・成功保持・時間切れを全刻み再計算する。`frame_chain.csv`、`actuation_chain.csv`へ結合結果を保存する。既存の回線・IP予算・復号・通知監査も全試行へ適用した。

`audit_reach_stage_conservation.py`で全撮影の終端を分類し、`frame_terminal.csv`へ保存。再構成後に取り出されなかった旧画像19件は、先に取り出された新画像の時刻と`JitterBuffer::PushFrame`の旧画像棄却条件から導いた。直接の棄却イベントを記録したものとは区別する。根拠となる新画像の番号と時刻も残し、不明な未取出し画像があれば合格にしない。

今回はC++本体を変更せず、G2-05最終exeを使用した。SHA-256：`19a9bd19919d5a0b0a6dbd81e3e25004a913d37aa76f7bc4387165dd908d555d`。既存の制御則・物理モデル・成功条件・電文は維持。真値は事後評価だけで使い、今回のPython処理を実時間の制御ループへ接続していない。

検証計画と結果：

- [事前計画](../artifacts/reach_g2_feedback_20260922/verification/integration_01/plan.json)の8条件を各60秒・中央初期姿勢で実行。正常T1/T2、上り断、下り断、容量低下＋8 KiBキュー、seed 206複合障害、映像全損失、下り300 ms。計測8/8合格、作業成功4・時間切れ4。成功後も共通60秒まで通信を記録した。
- 元の8 KiBキュー条件は初期IDRを復号器へ渡せず、再構成1,697に対し実画素0、1,200指令が`no_image`で実移動0。再構成したIDR4枚は新画像の取出しより遅く完成し、旧画像棄却条件に該当。残る1,693入力は`awaiting_initial_idr`で拒否された。この失敗を消さず保存した。
- 容量低下前の走行が成立する条件を切り分けるため、32 KiBキューの[追加計画](../artifacts/reach_g2_feedback_20260922/verification/integration_capacity_01/plan.json)を別途事前固定し、`verify_reach_capacity_transition.py`で実行。2秒以内の画素出力・走行、2～4秒の容量低下中のキュー破棄を確認し、作業は14.10秒で成功。追加を元の8条件への都合のよい置換にしない。
- 合計9/9計測合格、作業成功5・時間切れ4。16,200撮影・10,800指令・54,000物理刻み・10,800送信側推定・154,624,750 IP byteを照合。採用認識の誤差余裕超過0。正常T1は18.33秒、T2は10.61秒で成功。上り断／下り断も停止・再開後に成功した。
- 複合障害は再構成1,333→実画素66→認識採用50で時間切れ。下り300 msでは1,798画像を復号・認識しても、指令が100 ms期限を超え、動かず時間切れ。通信・映像の成功と作業成功を混同しない証拠として保持。
- [統合監査10改変](../artifacts/reach_g2_feedback_20260922/verification/integration_01/auditor_fault_tests.json)と[段階分類3改変](../artifacts/reach_g2_feedback_20260922/verification/integration_01/stage_conservation_fault_tests.json)をすべて検出。ネイティブソース720件のハッシュ照合が一致。同じC++版の単体、G2-04回帰8件、既存通信モードのG2-05保存証拠を再利用した。

計測上の注意：元の統合監査JSONの`control_loop_aoi_max_ms`は、停止指令にコピーされた不採用画像の番号も含む値。集計・表示では`source_tag_age_max_ms`とし、実際の`control_used`指令の適用中だけの古さを`control_used_image_age_at_application_max_ms`で別計算した。複合障害の36,192.644 msという番号の古さを、移動に利用した画像の古さとしない。元ログ・元JSONは保持した。

証拠：[元8条件の結果画面](../artifacts/reach_g2_feedback_20260922/verification/integration_01/index.html)、[追加容量試験](../artifacts/reach_g2_feedback_20260922/verification/integration_capacity_01/index.html)、[全9件の集計](../artifacts/reach_g2_feedback_20260922/verification/integration_01/aggregate.json)、[G2ゲート判定](../artifacts/reach_g2_feedback_20260922/verification/integration_01/gate_decision.json)。`finalize_reach_integration.py`が計画・実設定・全工程の整合・過去の個別証拠を確認し、監査コードも保存する。

状態変更：**G2-06完了、G2通過。G2は6/6、全体19/38、研究ゲートはG0・G1・G2の3/7。** 仕様v0.1.13、README、進捗表、説明書を更新した。次は**G3-01「B0固定・B1の実装と調整」**。同じ作用空間・予算・障害条件で強い期限／回線ベース方式を用意し、研究上の価値を比較できる状態へ進める。

限界：各条件1回の統合確認であり、5/9を統計的成功確率としない。G1全18姿勢の再実行、別PC・実網・実機の保証、完全参照グラフ、提案方式の優位性、アプリ／HTML全体の目視確認は未実施。G2合格は障害と作業結果を正しく説明できる計測基盤の判定であり、全障害で作業成功するという意味ではない。

## E018 — 2026-09-22：G3-01 B0固定・B1の実装と調整（完了）

対象ID：G3-01。着手時：G2通過、全体19/38、通過ゲート3/7。依頼は同じ通信予算・障害条件で比較できる強い既存方式の準備。実装意図・構造・前提は[G3比較方式説明](Reach_RT_G3_Baseline.md)へ記録。

既存RNVPのペーシング・帯域推定・適応FEC・期限付き再送を使うB0を研究プロファイルとして固定。B1は到着済みRNVPから損失・連続損失・RTTを推定し、実AU長と期限に対してFECなし／群2・4・8×再送要求上限0・1・2と見送りの13候補を評価する。詳細RSTA・作業真値・未来トレースは入力しない。共通IDR要求、ペーサ、IP予算、コーデック、制御器を使う。C++が実時間処理、Pythonが実験起動・独立監査・結果HTMLを担当。

初回v1は[20試行](../artifacts/reach_g3_baseline_20260922/verification/study_01/report.json)の記録監査が合格したが、意味の検証で不具合を発見。`PacketPacerStats::currentQueueDelayMs`は最後に処理したパケットの滞留時間であり、現在のキューを処理する時間ではなかった。B1の小キュー条件終盤では155,916 μsが残り、候補が永久に期限超過扱いとなった。[意味の検証結果](../artifacts/reach_g3_baseline_20260922/verification/study_01/semantic_review.json)を保存し、G3-01完了の根拠から除外する。失敗記録を削除・成功例へ置換しない。

v2では`PacketPacer::GetStats`でロック内の実キューpayload残量を集計し、現在レートから待ち時間を計算。既存の滞留時間統計は維持して別列へ記録する。実ペーサを空にしても過去値が残る状況を再現する試験を追加し、B1の将来待ち時間が0になることを確認した。既存ペーサの送信動作は変更せず、B0/B1の研究用入力を修正する。

v2のReleaseビルド成功。exe SHA-256：`43b0f0a92377b1ac9eb3848230b1620c763aef6ddbb9a108843ec89c86566859`。[単体](../artifacts/reach_g3_baseline_v2_20260922/verification/units_01/report.json)は425＋83＋145＋178＋81＋282＋679＝1,873検査合格。8秒の実通信確認で240判断・3,120候補、空キューに過去値が残る146判断を照合。[監査への10改変](../artifacts/reach_g3_baseline_v2_20260922/verification/study_01/auditor_fault_tests.json)を全て検出。

v2の調整条件はseed 301/302を維持し、λ=0/0.1/0.3/1/3を全て再調整する。別条件の障害seedは修正前に見た303/304から未使用の313/314へ変更し、正常T1/T2の回帰も含める。各60秒、調整12試行＋検証8試行、再試行なしの[新しい事前計画](../artifacts/reach_g3_baseline_v2_20260922/verification/study_01/plan.json)を保存して実行中。

v2は20/20試行の記録監査が合格し、調整でλ=0.3を選定。検証4条件の成功はB0が2件、B1が3件。ただし調整T2のλ=0.3は1,736/1,800判断が見送りで、空キューでも損失推定0.626445が残った。送信を見送るとTransportFeedbackが更新されず、高損失推定から抜け出しにくい点を強化対象とした。[設計見直しの記録](../artifacts/reach_g3_baseline_v2_20260922/verification/study_01/design_review.json)を保存し、20試行を保持する。

v3は、新規AUの提出が500 ms以上ないとき、既知の予算上限レート・実効λ=0で同じ13候補を評価する試験送信をB1へ追加。撮影期限・IP予算・有限キューを迂回せず、RSTA・作業状態・未来トレースを参照しない。B0/B1のIDR要求も間隔制限中に保留し、処理可能な時点で消費する。後続B2/B3/Rも試験送信規則を共有する。v2との差を試験送信だけの因果効果とみなさない。

v3 Release成功。exe SHA-256：`761b6d20d808ea1d0aa9cc3278b42938e4e6fc73a46d08ecc7c2e9162e1a2c7e`。単体は428＋83＋145＋178＋81＋282＋679＝1,876検査合格。8秒の実通信で240判断・3,120候補・2回の試験送信提出を確認し、予算・復号・通知・実動作を監査。監査への11改変を検出した。λの調整条件は301/302、検証障害は未使用の323/324として[事前固定](../artifacts/reach_g3_baseline_v3_20260922/verification/study_01/plan.json)。

v3の調整12件が合格し、検証開始前にλ=0.3を固定。正常T2のB0は46.716秒で撮影周期の取りこぼしを検出し、`camera deadline missed; historical pixels cannot be reconstructed`として無効終了した。原因は未特定。当初20件の`report.json`を`passed:false`のまま保持した。全20件の終了後、同じコード・係数・予算・条件で正常T2のB0/B1を追加する[別計画](../artifacts/reach_g3_baseline_v3_20260922/verification/supplement_01/plan.json)を保存。追加2件は作業成功・監査合格。元の有効B1も保持し、正常T2の対応比較には追加の両試行を使用する。元の無効記録の上書きや係数の再調整は行っていない。

最終版は22試行、完全監査した60秒窓21件、無効1件。[試行の扱い](../artifacts/reach_g3_baseline_v3_20260922/verification/study_01/trial_disposition.json)と[比較用集約](../artifacts/reach_g3_baseline_v3_20260922/verification/study_01/qualified_report.json)を分離。21件の完全窓で撮影37,800件・指令25,200件・物理刻み126,000件・候補351,000行・230,405,047 IP byteを照合し、採用推定の誤差余裕超過0件。試験送信提出396件、その画像の映像/FEC/再送費用3,475,018 IP byteを共通予算内で記録した。

調整後4条件の成功はB0が2/4、B1が3/4。正常T1は16.57/16.94秒、正常T2は12.92/11.87秒（B0/B1）。複合障害T1はB0が時間切れ、B1が49.91秒で成功。小キューT2は両方式が時間切れで、B1は通信量が増えても作業を完了できなかった。各条件1回の初期確認であり、統計的優位性や提案方式Rの価値は未判定。

最終exeで[復号8条件](../artifacts/reach_g3_baseline_v3_20260922/verification/decode_regression_01/report.json)、[状態通知8条件](../artifacts/reach_g3_baseline_v3_20260922/verification/state_regression_01/report.json)、[従来モード](../artifacts/reach_g3_baseline_v3_20260922/verification/legacy_regression_01/report.json)を再実行し、全て合格。従来モードは表示98枚を確認。単体1,876検査・監査への11改変検出に加え、固定exe・ソース・計測コード・選定手順・対応条件を照合して[完了判定](../artifacts/reach_g3_baseline_v3_20260922/verification/study_01/task_decision.json)を保存した。全18姿勢の再実行とGUIの目視確認は行っていない。

成果物として[比較HTML](../artifacts/reach_g3_baseline_v3_20260922/verification/study_01/index.html)、固定exe、全試行記録、再実行ツールを整備。説明書に実装意図・構造・判断式・公平性・モデルの限界・開発失敗履歴を記載し、仕様v0.1.14・進捗表・READMEを更新した。

**現在の状態：G3-01完了、G3は1/5、全体20/38、研究ゲート3/7。G3全体は未通過。** 次はG3-02「B2/B3による寄与分離」。B1と作用空間・試験送信・予算・調整機会を揃え、到着済みの復号情報／作業情報だけを追加した比較を成立させる。

## E019 — 2026-09-23：G3-02 B2/B3による寄与分離（完了）

着手時：G3-01完了、全体20/38、G3は1/5、研究ゲート3/7。依頼は同じ条件で復号情報と作業情報の価値を切り分けること。[実装意図・構造](Reach_RT_G3_Ablation.md)へ記録する。

B1と共通の13候補・IP予算・500 ms試験送信を維持し、B2には受信済みの同期状態・復号世代・直近復号待ち時間、B3には受信済みの作業段階・観測年齢・位置誤差余裕・期限を投影する。使わせない値は公開前にゼロ化。受信推定→マスク→公開→AU判断の番号と時刻を記録し、通知失効時はB1相当へ復帰する。同じ入力・同じλで状態価値を1へ戻したB1の選択も保存し、作業全体の再調整比較と一段階の選択差を区別する。

初版exe `c5696bb77f51b3ed6718f84622fdebb507a464fcb8e45abafb28fb225e48ebf3`は単体1,992検査が合格。旧設定テストの「B2は不正」を正規B2/B3・不正B4へ修正した。実通信9条件は機能監査が合格し、遅着の初回無効1件も保持。情報漏れ・未来参照等の10改変と、既存B1の11改変を全検出。

初版の調整`study_01`は時計違反4件の後に起動管理を停止し、その時点で動いていた5件目は完走させた。5件目B3は別監査で成功。係数選定はせず、[中断記録](../artifacts/reach_g3_ablation_20260923/verification/study_01/abort.json)を残した。実験プロセスだけをAboveNormalにした60秒B1確認は成功したが、別計画`study_02`ではB1成功後にB2が約56秒で撮影周期を取りこぼし、選定前に停止。優先度だけで解消したとは判断しない。

研究ループへオプションの高精度待機タイマーと、2 msを超えた各処理区間の記録を追加した。周期違反の検査・物理モデル・制御周期・撮影周期は維持。`reach_g3_ablation_v2_20260923`へ別ビルドし、初版を保存。高精度待機・AboveNormalのB2・60秒確認は全工程監査と作業成功を通過。記録された待機最大4.752 ms、撮影区間最大31.924 ms、通知poll区間最大22.558 msであり、余裕が無限にあるという証拠ではない。新版で単体・機能確認・同予算の調整を進める。

v2の長い調整は3件が成功した後、B2・λ=0.1で周期追随上限を超過。状態poll区間163.041 msと同時期のIP記録間隔164.138 msを観測した。v3では予算・回線の記録を上限128 MiB/ストリームのメモリへ移し、終了時に保存する。I/Oを根本原因と断定せず、共通ロック下のファイル書込みを除く変更とした。

v3は単体1,996検査・短時間9条件・監査改変検出が合格。長い調整の5件が60秒窓を完走したが、5件目B3・λ=0.1で、再構成済み画像420のデコーダ取出しが説明できず監査不合格。421は先に再構成されていたが、そのデコーダ取出しは420到着後だった。ジッタバッファから出す時点とデコーダが消費する時点を区別する記録が必要と分かった。

v4は既存のジッタバッファ棄却ガードで、先に送り出した画像番号を直接記録する。棄却・並べ替え規則は維持。ネイティブ単体と、正しい証拠／記録欠落・架空番号等の5改変で監査を確認した。旧v3記録は不合格のまま保存する。v4の単体2,000検査は合格したが、短時間B2通知断で物理区間775.740 msの停滞により無効終了。

v5では世界・指令・イベント等の共通`BufferedLog`も上限32 MiB/ストリームのメモリ記録へ変更。計測終了後に明示的に保存し、保存成功後に完了マーカーを作る。上限超過・保存失敗・強制終了を合格としない。Releaseビルドと単体2,000検査が合格し、新exeで機能・調整・検証を進める。全版で方式の重み関数はG3-02-v1のまま、係数選定前の停止であり、検証用seed 333/334はまだ使用していない。

v5のexe SHA-256は`2b12db7c79c254636ebc20b9d789a272b11358c2cb4fd477705e53906ec3da1c`。短時間9条件、情報漏れ等の10改変、既存B1監査の11改変、棄却証拠の5改変の検出が合格。[事前計画](../artifacts/reach_g3_ablation_v5_20260923/verification/study_01/plan.json)どおり各方式5係数×2条件で調整し、[選定記録](../artifacts/reach_g3_ablation_v5_20260923/verification/study_01/selection.json)を検証開始前の21:26:00 JSTに固定した。B1/B2はλ=0.1、B3はλ=1。調整用T2は全15試行が時間切れだったが、選定規則や障害条件は変更していない。

固定後の4条件ではB1が2/4、B2/B3が3/4成功。正常T1はB1/B2/B3の順に17.38/18.26/18.92秒、正常T2は11.65/10.24/10.30秒。複合障害T1ではB1が時間切れ、B2が36.52秒、B3が52.28秒で成功。小キューT2は全方式時間切れだった。同じ入力・同じλのB1相当から変わった判断は、各方式7,200判断中B2が1,743回、B3が1,527回。独立に調整した作業全体の比較と、この一段階の比較を混同しない。

調整30＋検証12の全42試行が監査合格し、無効・周期違反なし。撮影75,600件・指令50,400件・物理刻み252,000件・候補982,800行・455,562,264 IP byteを照合。採用推定の誤差余裕超過は0件。[集約](../artifacts/reach_g3_ablation_v5_20260923/verification/study_01/aggregate.json)と[比較HTML](../artifacts/reach_g3_ablation_v5_20260923/verification/study_01/index.html)を保存。旧版の無効試行の原因を外部負荷やディスクI/Oと断定せず、最終版の成績と分離して保持する。

最終exeの復号8条件・状態通知8条件・従来モード（表示85枚）の回帰も合格。`python tools/finalize_reach_ablation.py`で全試行・共通条件・係数選定・固定exe/ソース・単体/監査/回帰の証拠を照合し、[完了判定](../artifacts/reach_g3_ablation_v5_20260923/verification/study_01/task_decision.json)を保存した。仕様v0.1.15、進捗表、README、実装説明を更新し、固定条件による再実行ツールと比較画面の起動ファイルを整備した。

限界：各条件1回の初期比較。正常T1では追加情報による時間短縮がなく、小キューT2は解決できていない。B2の合計通信量減少には時間切れ中の見送り増加も含む。統計的優位性、両情報を併用するRの価値、G1全18初期姿勢の再実行、GUIの目視確認、実網・実機の性能は未確認。

**現在地：G3-02完了、全体21/38、G3は2/5、研究ゲート3/7。G3全体は未通過。次はG3-03「小規模モデルと厳密解」。** 因果的な最適解と事後オラクルを区別し、今回の情報が判断に価値を持つ条件と近似の限界を調べる。

## E020 — 2026-09-24：G3-03 小規模モデルと厳密解（完了）

着手時：全体21/38、G3は2/5、ゲート3/7。依頼は、情報が判断に価値を持つ条件と現行近似の限界を調べること。[前提・意図・構造・結果](Reach_RT_G3_Exact.md)にまとめた。

ライブC++は変更せず、`reach_exact_model.py`に有限IP鎖・2段階作業・2状態回線・遅延欠落通知・経路上の通信予算を持つモデルを実装。到着観測ごとにbeliefを更新し、有理数の動的計画法で終端成功確率を最大化する。同じ観測では同じ行動を選ぶ。現在の全状態を知る因果的上限と、全期間の未来を知る事後オラクルを別計算する。4候補WAIT/P/P_REPEAT/Iは小規模モデル専用で、ライブの13候補にIDR生成を追加したものではない。

165条件を固定して計算。基準例H=3・データ予算3・即時通知・通知欠落なしで、B1情報の最適解37/128、B2情報25/64、B3情報43/128、両情報101/256。全現在状態上限889/2048、事後オラクル2317/4096。通知が判断期限に間に合わない／全欠落／初期状態が既知の対照では、情報を増やしても値が変わらない。これらは設計した有限モデルの確率で、G3-02の実測成功率や提案Rの性能ではない。

同じ観測に対して今回の段階増分だけを見る近視眼的方策も実装し、最適解との差を記録。最大差はH=3・b=5・d=1・欠落なし・B3情報の13.28125ポイント。現行B3そのものの最適性差とは区別する。`reach_planner_probe.cpp`は現行C++ヘッダーの13候補を直接呼び、単一画像の全受信マスクとの63条件の比較を記録。余裕のある期限・独立損失では108候補の配送確率が一致した。一方、3チャンク・損失1/4・撮影後80 msの例で、平均IP量による期限判定は約95.385%を予測し、経路別の一括完了モデルでは約82.397%。現行重みvが実効λ/vを変える性質を15組で照合し、相関損失の9対照も保存した。

Hairpin原論文Appendix B.1の式6・8～11を確認し、理想MDSの60遷移分布と18再帰計算を全列挙で照合。公開実装全体・ブロックサイズ最適化・論文の性能値の再現とは呼ばない。9テスト群には、独立に全世界を生成する実装との16条件一致、未来通知の遮断、隠れた情報のマスク、確定的な成功不能、予算・期限単調性、上限拒否、手計算できる配送例を含む。165条件すべてで、近視眼的方策≤同じ情報の最適解≤全現在状態上限≤事後オラクルを確認した。

開発記録`development_01`はPath/PATH重複によりMSBuild失敗。子プロセス環境を辞書にして修正。`development_02`と`study_01`以降は合格。`study_02`で情報マスクと実装方策の違いを画面に明示し、予算別の結果を分数付き表へ整理した。`study_03`で小数のスロット数等を拒否する入力検査を加え、再度全条件を実行。旧出力は保持した。

最終コマンドは`python tools/run_reach_exact_study.py --name study_03`。`python tools/verify_reach_exact_artifacts.py`で証拠64・ソース51ファイルを照合し、前版165条件の分数・判断と全診断結果が一致した。[完了判定](../artifacts/reach_g3_exact_20260924/study_03/task_decision.json)、[再現性](../artifacts/reach_g3_exact_20260924/study_03/reproduction.json)、[結果HTML](../artifacts/reach_g3_exact_20260924/study_03/index.html)を保存。コード・ヘッダー・入力・出力・実行環境を同梱し、説明書・仕様v0.1.16・進捗表・READMEを更新した。

限界：実H.264の完全な参照関係、画像AoI、実認識・制動・指令・可変IDR生成時間は小規模モデル外。通知64 IP byteもモデル専用。最大確認は4スロット、最大探索887ノードで、それ以上が計算不能とは主張しない。単発Python時間は実時間保証に使わない。ライブG1/G2/G3-02試行の再実行やGUI目視確認は行わず、以前のライブエンジンのソース・exeが不変であることを確認した。

**現在地：G3-03完了、全体22/38、G3は3/5、研究ゲート3/7。次はG3-04「H1の反例と成立領域」。** 同じネットワーク統計・AoIで判断が変わる状態対、予算・期限・参照状態の境界を詰める。今回の差だけで一般的な新規性や実環境での優位性を結論しない。

## E021 — 2026-09-24：G3-04 H1の反例と成立領域（完了）

対象ID：G3-04。未着手→作業中→検証→完了。目的は、通信統計と画像の古さだけでは判断しきれない状態対と、予算・期限・参照状態による95%成立範囲を明確にすること。G3-03のソース・証拠とライブエンジンを変更せず、Python標準ライブラリによる独立した理論実験を追加した。

`reach_h1_model.py`に撮影時刻と参照保持を別々に持つモデルを実装。同じ画像を受信して制御側が保持し、一方だけ復号器の参照を消去する履歴対を構成した。パケット履歴・AoI・作業段階・未来の回線分布・予算を一致させ、到着した参照通知の有無だけを比較する。AGEにも撮影時刻・ACK・作業段階と全履歴を与えるため、共通情報からの参照推測を禁止しない。参照消去の確率はモデル上の設計値で、実機で同定したものではない。

最小例は残り1段階・1スロット・映像2パケット、独立到達確率0.9、参照保持事前確率1/2。参照なしではIが唯一最適、ありではP_REPEATが唯一最適で、最適行動集合は交わらない。AGEは81%、REFは90%。一般のp・事前確率πに対する解析式と、厳密に正の情報価値になる条件も説明書へ記載した。通知未到着・全欠落、予算不足、参照が既知で共通情報から追跡可能な条件では価値が消える。

事前条件は最小例1＋解析式105＋通知条件18＋総予算・期限等216の340条件。316条件を厳密計算し、通知予約だけで総予算を超える24条件は実行不能として保存。残り2段階・3スロット・総6,656 IP byteでAGE 94.041%／REF 96.39%、REFのみ95%成立。試験格子上の最小総予算はAGE 8,192 byte、REF 6,656 byte。固定総予算では期限延長時の通知予約増加により映像パケット数が減る境界も保存し、無条件な期限単調性や未試験点の補間を主張しない。

新規9テスト群が合格。ソルバーの遷移・観測関数を使わない全世界列挙で、H=2の48比較、95%境界を跨ぐH=3の2比較の合計50を検算し、成功確率と期待費用が分数で一致。全340条件を繰り返し再現、最大探索177ノード。G3-03の9テスト群も回帰合格。G3-03ソース51・証拠64ファイル、G3-02ライブソース・exe不変を確認した。

開発`development_01`は1スロット解析式を残り2段階にも適用した検査で失敗。ソルバーではなく検算条件を修正して失敗記録を保持し、`development_02`で合格。最終`study_01`ではH=3の独立検算と色付き成立表を追加し、再実行した。`python tools/run_reach_h1_study.py --name study_01`と`python tools/verify_reach_h1_artifacts.py`が合格。新規証拠13・ソース5ファイル、全340条件、HTMLリンク7件を検証した。ブラウザ目視確認は未実施。

[実装意図・構造・前提・証明・限界](Reach_RT_G3_H1.md)、[結果HTML](../artifacts/reach_g3_h1_20260924/study_01/index.html)、[完了判定](../artifacts/reach_g3_h1_20260924/study_01/task_decision.json)を保存。仕様v0.1.17・進捗表・READMEを更新した。数学モデルでの条件付きH1支持と、ライブ方式の効果・新規性・統計的優位性を分離する。

**現在地：G3-04完了、全体23/38、G3は4/5、研究ゲート3/7。次はG3-05「新しい状態情報の価値を判定」。** G3-01～04の強い比較・理論結果・近似の限界・負の結果を統合し、継続する仮説とG4で必要な検証を明示してG3全体を判定する。

## E022 — 2026-09-24：G3-05 状態情報の価値の判定・G3通過

対象ID：G3-05。未着手→作業中→検証→完了。実測比較・理論結果・負の結果を統合し、継続する仮説とG4の実装方針を明確にするという依頼に対応した。

判定は、H1を限定モデルで条件付き支持、H2を未実証、H3を限定モデルの実例のみ・ライブ未実証、H4を未実証・近似の不足を特定として継続。G3-02の複合障害T1の改善、小キューT2の全方式失敗、正常T1での完了時間悪化を残した。B3とB1のλの違い、各条件1回、同一入力反実仮想と閉ループ結果の違いも明記。G3-01の無効1試行・追加対応試行と、版間の係数・exe差を維持し、成功数を合算しない。

G3-03基準例で、両情報から復号情報のみを差し引いた作業情報の条件付き増分1/256（0.390625ポイント）を確認。G3-04の81%→90%の状態対、94.041%→96.39%の95%境界を、実測成功率と分離して扱う。通知未到着・全欠落、参照推測可能、予算不足の無効果条件も統合した。

G4は復号情報を中心に、候補別の参照回復→後続画像→作業利用を予測する方針とした。現行の重みvが実効λ/vの変更に相当するため、係数調整方式を対照として残す。G4-01の5操作、IDR要求と実出力の分離、実行直前の期限・予算再検査、G4-02の校正と範囲外、G4-03の5 ms初期打切りと代替、G4-04の同候補情報マスク、G4-05の固定条件を[実装計画](Reach_RT_G4_Implementation_Plan.md)へ記載した。G4のコードは未着手。

新規`tools/finalize_reach_g3.py`は旧証拠を読み取り、原JSONから対応結果と理論値を集約し、新しい出力先へゲート記録を保存する。`development_01`、最終`review_01`とも合格。原証拠60/63/64/13ファイル、理論ソース51/5、ライブソース87、履歴exe2本を照合し、重複を除く306入力の整合を確認。4条件の実測対応比較、H2の条件付き理論増分、H1の最適行動分離・95%境界・通知無効果対照10条件が一致。今回は新たなライブ試行、全通信生ログの再監査、数学ソルバーの再実行、統計的検定を行ったものではない。

[判定書](Reach_RT_G3_Decision.md)、[集約結果](../artifacts/reach_g3_gate_20260924/review_01/report.json)、[G3ゲート判定](../artifacts/reach_g3_gate_20260924/review_01/gate_decision.json)、[説明用画面](../artifacts/reach_g3_gate_20260924/review_01/index.html)を保存。新規監査ソースと判定・実装計画のスナップショット、証拠ハッシュを同梱した。仕様v0.1.18・進捗表・READMEを更新した。

G3の規定である「新しい情報の判断価値、または負の結果を特定し、不足と改善を具体化」を満たすため**G3通過**。全仮説の実証やRの優位性・学術的新規性の認定ではない。効果が消える場合の縮小・不支持条件も明記した。

**現在地：G3-05完了、全体24/38、G3は5/5、研究ゲート4/7（G0～G3）。次はG4-01「有限候補生成と実行接続」。** キャッシュ・FEC・出力済み画像・IDR待ちを扱い、実行可能な操作を共通予算へ接続する。

## E023 — 2026-09-24：G4-01 有限候補生成と実行接続（完了）

対象ID：G4-01。着手時は全体24/38、G4未着手、ゲート4/7。依頼された再送・FEC・新規画像・IDR更新・見送りを、存在するデータと共通通信予算へ結び付ける実行層として実装した。[目的・構造・説明用要約](Reach_RT_G4_Actions.md)を保存。

`ReachActionModel.h`にDEFER/FRESH/PROTECT g2・g4・g8/REPAIR/REFRESHの7候補、費用、可否・却下理由を追加。最新4画像のメタデータ、実出力AU、受信済み選択再送要求、実キャッシュ・XOR生成可能性、撮影後200 ms期限、ローカル世代を使用。`ReachActionExecution`が既存RNVP送信・再送・実XORとエンコーダ要求を接続し、通常プロトコルと研究用IDR要求を同じ500 ms窓・出力待ちで調停する。生成要求を画像送信や復号成功に数えない。

実送信直前に世代・メタデータ・期限・再送認可を確認し、世代ロックを共通予算検査・sendtoまで保持する。`BudgetTransport`では期限付き呼び出しだけ予算ロック後にも期限を検査し、既存の全通信へのbyte・レート制約を維持。候補費用は予約ではないので、手配後の部分送信・予算棄却を実通信ログで区別する。終了時はペーサを停止・合流してからログを閉じる。認識・制御・作業成功条件は変更していない。

設定`baseline.mode="G4-01"`は接続確認用。現在の選択はPROTECT g4→FRESH→DEFERの固定規則と合法な再送・IDR要求であり、到着・復号・作業価値の予測を実装したものではない。今回の入力はイベント駆動。RSTAを運ぶ既存回線は維持するが、通知・キューを予測入力へ拡張するのはG4-02、20 ms周期・5 ms打切り・代替はG4-03へ接続する。G3-05の固定計画は歴史的記録としてそのまま保持し、具体化した範囲と残工程を新説明書へ明記した。

`build_02`のReleaseビルドと新規単体684検査、`regression_units_01`の既存単体2,000検査が合格。exe SHA-256は`8a88f1f30eca889d1fc43ed5f86a8e863d6784c28303ab8b63424079e276083a`。新規試験・最終集約は`tools/build_reach_actions.py`、`verify_reach_actions.py`、`finalize_reach_actions.py`で再実行可能。

最終`acceptance_02`の各8秒・T2の7条件（正常、部分損失、byte不足、小容量／有限キュー、送信待ち、下り通信断、P画像欠損）は全監査合格・推定誤差余裕超過0。実再送を受信済み欠損要求へ照合し、実XORをデータから独立再計算した。送信待ちでは世代変更31件・メタデータ消失31件を遮断。下り断後の制動停止も確認した。6種類のログ改変を全検出。既存B1〜B3の9条件、B0正常1条件、従来モード（実復号記録142・表示98枚）も合格。

開発6条件の`acceptance_01`と初回pilot記録は保持。pilot初回は監査側のRNVP stream ID読取オフセットが8だったため失敗し、正しい12へ修正して再監査合格。最終版には送信待ち条件、実予算台帳の送信時刻での期限、認可種別・最新世代の検査を追加した。最終7条件の計画は開始前保存、再試行なし。

[結果HTML](../artifacts/reach_g4_actions_20260924/verification/final_01/index.html)、[完了判定](../artifacts/reach_g4_actions_20260924/verification/final_01/task_decision.json)、[集約](../artifacts/reach_g4_actions_20260924/verification/final_01/aggregate.json)を保存。全試行のソースコピーとビルド時ソース・exeを照合し、候補監査・6改変検出を再実行、入力ハッシュと新ソースを保存する。G3の固定出力7ファイルの不変を確認。仕様v0.1.19、進捗表、READMEを更新し、`tools/open_reach_g4_actions.cmd`で結果画面を開けるようにした。

限界：短時間の接続確認であり、Rの優位性・作業成功率・全18初期姿勢の再実行・GUI目視・実網性能の証拠ではない。パリティ単独追加や4画像の組合せ最適化も未実装。旧G3実測のexeとは変更があるため成績を混ぜない。

**現在地：G4-01完了、全体25/38、G4は1/5、研究ゲート4/7。G4全体は未通過。次はG4-02「到着・作業価値の予測と校正」。** 候補ごとの到着・参照回復・後続画像・作業利用を開発用データだけで校正し、未知・失効・範囲外を明示する。

## E024 — 2026-09-24：G4-02 到着・作業価値の予測と校正（完了）

対象ID：G4-02。着手時は25/38、G4が1/5、ゲート4/7。到着・参照回復・作業利用に役立つ候補を予測するという依頼に対し、C++の有限窓予測器と分離した校正・誤差監査を実装。[実装意図・構造・説明用要約](Reach_RT_G4_Prediction.md)へ記載した。

`ReachPrediction.h`は同じ操作・FEC群・IDR・参照区分・作業段階・タスクに属する過去の実測経路から、16数値特徴が近い最大64件を使う。8件・2学習試行に満たない場合は範囲外。200 ms窓の画像利用可能・復号・参照状態・対象／後続画像の指令利用・新規作業完了の6値と、到着／復号／指令のp50・p90を出力する。未定義を0とせず、未通知・250 ms失効・世代未確認・校正不足を記録。真値は事後ラベルだけに使う。

G4-02モードはFRESH/g2/g4/g8を固定巡回し、共通の再送・IDR要求処理を維持する。予測は選択へ使わず、候補と判断番号を対応付けた`predictions.csv`へ出す。通知番号・採用時刻・フィードバック番号・IP費用・ローカル待ちを保存。固定継続規則の下での条件付き予測なので、候補間の因果的寄与や60秒成功率と混同しない。制御器・認識・物理・成功条件は変更していない。

初回build_01はWindowsのgetenv警告のエラー化で失敗し、APIを修正。build_02で各20秒の開発18試行を収集後、すでに利用可能な画像を残り待ち0とする定義と、校正後の事象包含関係・DEFERの構造的0を修正した。12,291学習／校正候補のうち328件に既存利用可能経路があり、画像・復号・指令の包含違反0を確認。初回記録は保持。

最初の8試行8,227経路だけを学習へ、次の4試行4,064判断を校正へ使い、v2モデルを再固定。校正は固定5区間、16件・2試行の支持を要する。過去の検証・ストレスの正解を再学習へ入れず、build_03で新しい検証4＋ストレス2試行を`study_02`へ記録した。最終の役割別集合は18試行、実収集は旧18＋新6の24試行。初期条件1組・単一PC内の開発検証であり、G5保留テストではない。

新しい検証は、通知が利用可能で窓末尾まで観測できる4,095判断中、範囲内3,870・範囲外225。ストレスは1,145中142が範囲内、1,003が範囲外。原ログでは未通知・失効も別計上する。同一対象集合のBrier scoreは画像0.026036→0.026702、復号0.063540→0.065891、参照0.158911→0.161012、対象指令0.133688→0.135867、後続指令0.132910→0.137375、新規完了0.004024→0.004120。**5区間校正は全項目で誤差を改善しなかった**。完了正例16窓には同じ完了イベントの重複窓が含まれ、高精度な成功確率予測の証拠とはしない。

修正版exe SHA-256は`83bbd3777120c3ce176baf1d91fc48595c97517ccb0f4550e18d84ddc041d76e`。予測C++37検査・実行層684検査・既存単体2,000検査、G4-01実UDP 7条件、B3正常・従来モード（表示98枚）の回帰が合格。独立Python実装で近傍・確率・経路分位点・校正表を検算し、実行時と再実行の予測を照合する。初回監査は初期フィードバック未到着時の損失特徴改変を検出できず失敗し、初期値0.01の照合を追加。原データを変えずに6種類の改変検出を再確認する。

[完了判定](../artifacts/reach_g4_prediction_20260924/verification/final_01/task_decision.json)、[誤差・監査JSON](../artifacts/reach_g4_prediction_20260924/verification/final_01/aggregate.json)、[結果HTML](../artifacts/reach_g4_prediction_20260924/verification/final_01/index.html)を保存。入力・モデル・ソースと各試行のコピーを照合し、旧G4-01成果物は保持。仕様v0.1.20、進捗表、READMEを更新し、`tools/open_reach_g4_prediction.cmd`を追加した。

限界：校正改善、操作の因果的寄与、60秒成功確率、他PC／コーデック／初期姿勢への汎化は未実証。20 ms周期・計算打切り・予測に基づく選択は未実装。完了判定は、予測・分離校正・範囲外・誤差を検証可能にしたことに対するもので、予測精度やRの性能合格ではない。

**現在地：G4-02完了、全体26/38、G4は2/5、研究ゲート4/7。次はG4-03「ReachSchedulerと代替処理」。** 校正表を無条件採用せず、選択規則変更に伴う分布変化、範囲外・計算超過時の代替、計算時間を扱う。

## E025 — 2026-09-25：G4-03 ReachSchedulerと代替処理（完了）

**依頼・意図。** G4-02の校正改善がなかった事実を踏まえ、実行可能な候補を20 ms周期で選択し、計算打切りと範囲外の代替へ接続する。未校正の200 ms内の対象画像指令利用頻度は、費用と合わせた実験用の順位付けに限定し、作業成功率・因果効果とは扱わない。

**実装。** C++の`ReachScheduler.h`と`ReachActionExecution`へ、最新4画像・最大22候補、専用スレッド、高精度タイマー、5 ms協調打切りを追加。予測器内部では64経路行ごと等で中断を確認し、部分的な推論値を採用しない。未通知・失効・支持不足・モデル未指定・打切りでは、受信済み要求に基づく再送、IDR要求、最新画像、DEFERへ戻る。選択後の世代・200 ms期限・実キャッシュ・予算を再検査し、変化していればDEFER。実送信時の共通IP計上も維持した。

周期番号からスナップショット、全候補の特徴・予測・選択理由、実行認可、実UDP、復号・認識・指令・物理結果へ追跡する3種類のスケジューラCSVを追加。選択中は送信・通知の共通ロックを保持しない。再送は受信時に保留し、選ばれた時点で実送信キャッシュを読み直す。IDR予約は次のエンコーダ入力で発行し、実出力との対応を保つ。既存G4-01/02とB0～B3の起動経路は独立したまま維持した。

**検証。** 最終`build_03`で、単体233＋予測37＋実行層684＋既存2,000＝2,954検査が合格。通常T1/T2と損失各20秒、予算・キュー・低速ペーサ・逆方向断・強制打切り・モデルなし・P画像欠落各8秒の10条件を監査。計5,790判断・126,355候補行、各条件p95最大1.429 ms／p99最大2.074 ms、周期スキップ0、判断時間5 ms超過0。独立予測65件、監査6改変検出が合格。採用認識の誤差余裕超過0。逆方向断では2.96秒でwatchdogが発動し、4秒以降の速度0を確認した。

G4-01実UDP 7条件、G4-02の予測・因果入力、B3、従来表示98フレームの回帰も合格。G1全18姿勢やGUI全体の目視確認、他PC・実機は再検証していない。強制打切りは分岐の診断であり、実CPU過負荷の成績ではない。判断時間とRNVP投入時間を分け、詳細候補ログの整形時間は判断時間に含めない。Windows上の硬い時間保証はしない。

**開発・訂正記録。** `pilot_01`は`build_01`で20秒、999判断・スキップ0。開始遅れp99 16.154 msを改善するため高精度待機タイマーへ変更し、最終10条件では0.803～1.920 ms。`build_02`では実通信を実行していない。予算条件の最初の監査は棄却候補を実行認可ログから探したため失敗し、全候補ログを確認するよう修正した。元報告を保存し、同じ生データを再監査。最終10試行の再実行・結果除外は行っていない。

[実装説明](Reach_RT_G4_Scheduler.md)、[結果画面](../artifacts/reach_g4_scheduler_20260925/verification/final_01/index.html)、[監査集約](../artifacts/reach_g4_scheduler_20260925/verification/final_01/aggregate.json)、[完了判定](../artifacts/reach_g4_scheduler_20260925/verification/final_01/task_decision.json)を追加。仕様v0.1.21、進捗表、READMEとG4-03結果／ライブ起動ファイルを更新。G4-02の固定証拠・実装計画は保持した。

**現在地：G4-03完了、全体27/38、G4は3/5、研究ゲート4/7。次はG4-04「厳密解との比較・アブレーション」。** 同候補の情報マスク、係数調整対照、近似誤差・分布変化・無効果／悪化条件を調べる。今回の実装合格から、作業成功率の改善や提案Rの優位性は結論しない。

## E026 — 2026-09-25：G4-04 厳密解との比較・アブレーション（完了）

**依頼・意図。** G4-03の選択が有効な条件と、近似によって悪化する条件を分ける。同じ候補で情報だけを変える比較、等しい機会で調整する閉ループ比較、同じ因果的観測を使う小規模厳密解との比較を別々に実装した。

**実装。** C++予測器へcommon/decode/task/jointの情報マスクを追加。隠したカテゴリ・特徴を問い合わせ側と学習照合側の両方から除く。scalar対照は共通情報の予測に受信済み状態の固定重みを掛け、係数変更に相当する効果を切り分ける。共通順位付け関数`ReachRanking.h`をliveと有限モデル照合で共有した。G4-04モード、係数・マスク記録、ネイティブ再評価器、事前固定する比較計画・結果画面・起動ファイルを追加。認識・指令・物理・成功基準、20 ms周期・5 ms協調打切り・再検査は維持した。

**実測。** `artifacts/reach_g4_comparison_20260925`の`build_01`で、調整32＋固定後72＝104試行すべて計測監査合格。主比較42試行は20秒、診断30試行は8秒。5マスク方式とB1はλ=1、B2/B3はλ=0.1。ただし5マスク方式とB1の調整条件は両係数とも成功0で、IP量による選定となった。検証条件を調整へ戻さず、取り直し・失敗除外はしていない。別途の開発試行`pilot_common_01`は成績へ混ぜない。

通常T1ではjoint 17.92秒、task 19.97秒、common/decode/scalarは20秒以内に未完了。既存B3は16.61秒でjointより速い。通常T2ではcommon 10.59秒、joint 11.96秒、B3 10.25秒。IDR欠落・P欠落・認識停滞は5方式すべて完了、小キュー複合障害はB0～B3を含む9方式すべて未完了。各条件1試行なので成功率改善・統計的優位性とは呼ばない。

**寄与・厳密比較。** 同じ1,338状態を8,028回再評価し、λ=0.1で情報の寄与を比較。実測係数では、時間打切り113状態を除く1,225状態の選択・代替理由が一致した。通常T1のjoint対decodeは双方で予測支持がある90状態中62で判断が変わるが、判断差を作業改善の因果効果と断定しない。低速ペーサは全判断が代替処理となり、同一候補でも情報追加の差はなかった。

有限モデル40条件×4情報集合の160比較は、短期選択が同等82・悪化78。成功確率の平均差4437/327680（約1.35406ポイント）、最大9/128（7.03125ポイント）。予測を正確にしても、最初の画像に予算を使い切って次工程へ送れない反例を保存した。独立全世界列挙16＋16件と、到達した順位付け全34入力の実C++照合が合格。抽象WAIT/P/P_REPEAT/Iを実RNVPのXOR・非同期IDRと同一視しない。

**時間・回帰。** 61,920判断、周期スキップ0。各試行p95最大1.901 ms／p99最大3.195 ms。自然な5 ms超過5件を残し、5 msを硬い実時間保証としない。IDR/Pの実型、実認識停滞250 ms以上、通知逆転各39件、逆方向断後の停止を確認。単体2,000＋684＋37＋233＋126＝3,080検査、独立予測165問、G4-01実UDP 7条件、G4-02・G4-03・B3・従来表示の回帰が合格。元のG4-03固定証拠97ファイル、モデル・exe・ソースの対応と監査6改変検出を最終集約で照合する。

[実装説明](Reach_RT_G4_Comparison.md)、[比較画面](../artifacts/reach_g4_comparison_20260925/verification/final_01/index.html)、[集約](../artifacts/reach_g4_comparison_20260925/verification/final_01/aggregate.json)、[工程判定](../artifacts/reach_g4_comparison_20260925/verification/final_01/task_decision.json)を追加。仕様v0.1.22、進捗表、READMEへ反映。GUI全体の目視、G1全18姿勢再実行、他PC・実網・実機での性能は未確認。短期予測の実測誤差は方式ごとに選択集団が異なり、精度優位性の証拠として使わない。

**現在地：G4-04完了、全体28/38、G4は4/5、研究ゲート4/7。次はG4-05「予備実験の終了判定」。** 通常T1の情報価値を継続検証する条件と、通常T2・小キュー・予測範囲外の負の結果を残し、主比較・設定・試行数案・無効条件・実行時間見通しを固定する。G4全体とRの優位性は未認定。

## E027 — 2026-09-25：検証画面へのボタン導線

依頼は「検証画面をボタンで切り替えたい」。G4-04／03／02の結果を切り替えるHTML、通常アプリの常設Resultsボタン、研究モードで計測終了後に有効になるボタン、閲覧用／更新版アプリ用の起動ファイルを追加した。保存済み結果を読む表示層で、選択器・通信・作業条件には変更を加えない。UI追加用のexeは`artifacts/reach_viewer_20260925`へ分離し、元の実験exeと成果物を保持した。

Releaseビルド、3結果の実在・固定証拠104／97／95ファイルの照合、JavaScript構文とDOM代替による切替処理を確認。初回の直接ビルドはPath/PATH重複で失敗し、Pythonから正規化した環境で再ビルド成功。ブラウザー操作はfile URLへのアクセスをセキュリティポリシーで拒否され、実画面の目視は未確認。通常映像モードの短時間回帰は復号141記録・表示96枚・正常終了で合格し、`verification/navigation_smoke_01`へ記録した。詳しい操作・実装意図は[検証画面説明](Reach_RT_Verification.md)。工程数は28/38、G4は4/5、研究ゲート4/7のまま、次はG4-05。

## E028 — 2026-09-25：Visual Studio F5からの検証画面導線

ユーザーの意図はVisual Studioから起動したアプリでの操作だったため、cmd起動を必須とする案内を訂正。GE3のローカルデバッガー起動先を`$(TargetPath)`、作業フォルダーを`$(ProjectDir)`とし、ヘッダーをプロジェクト・フィルターへ登録した。再ビルド→F5→Network Lab上部の「Reach-RT Results」→保存結果の工程切替という手順をREADMEと画面説明書へ記録した。研究ライブ画面の切替は、このResultsボタンでは実装していない。

Debugの確認ビルドで`ReachFoundation.cpp`のBCryptHash未宣言を検出。DebugのWindows 8対象設定を保ち、同一のSHA-256を計算するBCryptCreateHash／HashData／FinishHashへ修正。旧エラーログを保持し、修正後のDebug x64ビルドが合格。Windows対象0x0602で基盤681検査を再実行し、既知ハッシュと設定・時計・記録の検査が合格。Debug exeの2秒headless起動、exe・入力設定・実効設定のハッシュをPythonと照合した。検証は`artifacts/reach_vs_ui_20260925/verification`、通常F5の出力先は不変。Visual Studio UIのF5操作や目視は実施していない。

研究工程はG4-04完了、28/38・G4 4/5・ゲート4/7のまま。次はG4-05。今回のDebug互換修正を過去の実験exeの性能再認定には使わない。

## E029 — 2026-09-25：通常画面と研究実行画面の同一プロセス切替

ユーザーの依頼は、Visual Studioから通常起動したアプリ内のボタンで、実際に動く研究画面へ切り替え、戻れるようにすること。Network LabとF1非表示時の導線へ「Research Live」を追加し、研究側へ「通常画面へ戻る」を追加した。保存済みHTMLを開くResultsは別の機能として保持する。

`AppMain::Run`を外側の切替ループと`RunLegacy`へ分け、通常側の通信・描画資源を終了してから研究を実行する。新しい`ReachLiveNavigation`が設定・予測モデルを確認し、環境変数を一時保存・復元する。別exeやPythonを起動せず、同じF5プロセスを使う。研究はT2・joint・λ=1・正常回線・20秒で、H.264／RNVP／UDP指令／仮想ロボットを実行する。中断は`interrupted`、×はアプリ終了、通常へ戻った際の一時UI設定は初期化する。戻り時の残存WM_QUIT、ウィンドウ再登録、全構成のImGui入力・終了処理も修正した。

実表示の開発試験`navigation_02`は描画待ちで時計追従上限に達し、エラーダイアログ待ちでタイムアウト。UIスレッドが所有する画像・状態コピーを描く構造に分離した。`navigation_03`は約6.57秒で撮影周期の取りこぼしを検出。Debugの画素生成が1枚約36 msとなるため、`ReachRobotWorld.cpp`だけ速度最適化し、計測ガードを維持した。失敗ログは保持し、固定済み研究結果に混ぜない。

最終Debugビルドの`navigation_04`は4条件合格。実画面20秒×2回は各物理2,000／撮影600／制御400刻み、復号画像識別一致565枚・570枚、実移動とスケジューラ記録、描画195回・196回を確認。同じPIDで通常→研究→通常→研究→通常を完了。短時間完了・途中中断・モデル不足も各2回復帰した。従来の環境変数起動を2秒確認し200／60／40刻みで完了。exeのSHA-256とビルド時ソース一覧を照合した。Visual StudioのF5操作・マウス入力・レイアウト目視、Release版、全初期姿勢の再認定は未実施。

成果物は[操作・実装説明](Reach_RT_Live_Switch.md)、`config/reach_rt_live_demo.json`、`tools/verify_reach_live_switch.py`、`artifacts/reach_live_switch_20260925/verification`。通常VS出力先や過去の固定実験exeは上書きしていない。実行完了は作業成功率の再認定ではない。**現在地：G4-04完了、全体28/38、G4 4/5、ゲート4/7。次はG4-05「予備実験の終了判定」。**

## E030 — 2026-09-25：Developmentの撮影期限超過を調査・修正

依頼は、研究実行開始から数秒で発生した `camera deadline missed; historical pixels cannot be reconstructed` の原因調査。23:25のセッション `090E4BD8-032E-4AAD-BCB7-2F89AE92A7DC` と実ビルド履歴を確認。起動exeはDevelopmentで、E029のDebug限定の最適化が適用されず、`ReachRobotWorld.cpp` が `/Od` でコンパイルされていた。

画像生成は最初30枚の中央値7.556 msから最後30枚32.740 msへ増加し、最大35.845 ms、11枚で30 Hzの間隔を超過。約8.170秒で撮影予定が2回分たまり、計測保護の検査で停止した。撮影キュー最大1、復号失敗・識別不一致0であり、直接の例外原因は画像生成側の遅れ。OS・デバッガーの上乗せ時間までは分離できない。

対象ファイルだけの速度最適化をDevelopmentにも適用し、画像式・撮影周期・期限検査を維持。隔離したDevelopmentビルド成功と `/O2` を確認。`navigation_01`の実画面20秒×2回は各600枚生成・2,000物理刻み・400制御刻み、復号562枚／488枚、移動、同一PIDでの往復が合格した。生成時間中央値6.775 ms／6.749 ms、最大19.271 ms／32.425 ms。VSデバッガーは接続しておらず、その状態の時間保証や研究成功率の再認定ではない。

[調査説明](Reach_RT_Live_Deadline_Investigation.md)と `artifacts/reach_live_development_20260925/verification` に元ログのハッシュ・原因集計・コンパイル指定・修正後の実行を保存。通常VS出力exeと元失敗ログは保持し、ユーザー側で再ビルドする。**現在地はG4-04完了、28/38、G4 4/5、ゲート4/7。次の研究工程はG4-05。**

## 今後の追記テンプレート

```text
## E番号 — YYYY-MM-DD：作業名

対象ID：
着手時の状態：
行ったこと：
変更したファイル・成果物：
検証したコード状態・設定：
実行したコマンド / ログの保存先：
結果：
未確認事項・失敗・阻害要因：
タスク状態の変更：
仕様変更の有無と理由：
次に行うこと：
```
